# Current MS2.0 live validation state

Canonical sequence authority: `../../MS2.0_MASTER_LIVE_TEST_SEQUENCE.md`. This file records evidence and the current pointer only; it does not maintain a separate roadmap.

Final completeness audit: the master contains 76 unique checkpoints, an evidence-backed domain ledger, acyclic numeric prerequisites, and the sole execution pointer. Historical embedded sequences remain evidence only; future chats and Bridges must read the master at the current commit.

Permanent traceability rule: checkpoint IDs, evidence, implementation commits/files, owner proof, remaining work, prerequisites and dependents live in the master’s generated Engineering Traceability Index. Update it with `node scripts/update-master-traceability.mjs` and require `node scripts/verify-master-traceability.mjs` to pass in the same commit as any checkpoint or evidence change.

## Export Hub checkpoint — 2026-07-27

- Owner evidence makes Excel, PDF, Word and the nine-slide Presentation **OWNER-VALIDATED PASS** and regression-protected. Presentation downloaded, was detected correctly, opened in an Android standards-compatible reader, and displayed all nine readable slides without corruption. The original Android `Error (4)` blocker is resolved. PowerPoint is recommended, not required.
- Exactly one persistent compact `ExportHubCard` exists per pharmacy. It updates in place; full newest-first deduplicated history stays inside Export Hub; ordinary chat-feed export messages are prohibited.
- `exportFormatMetadata.js` is authoritative for purpose, applications/fallbacks, next action, completion/regeneration wording, icon, MIME/extension, behavior, safety, accessibility and expiry.
- CSV Technical Data Transfer is **OWNER-VALIDATED PASS** and regression-protected. Final Google Sheets evidence confirms the fresh UTF-8-without-BOM file opens as a genuine spreadsheet, cell A1 is exactly `Medicine`, all 35 medicines remain one per row, all 12 canonical columns stay separated/aligned, and blanks do not shift later values. Download/history, the concise single status card and clean chat remain passed.
- The canonical CSV is frozen: UTF-8 without BOM, exact header-first 12-column schema, one CRLF-terminated physical row per medicine, normalized embedded line breaks, standards quoting, formula-injection protection, leading-zero barcode preservation, pharmacy isolation and zero-AI generation. Do not revisit without regression evidence.
- Print Working Inventory is **OWNER-VALIDATED PASS** and regression-protected. Owner evidence confirms the complete 35-medicine view, native Android preview, four correct pages (1/4 through 4/4), no blank/corrupt page, no crash, no printer selection, one compact Print history entry, one updated status card and no new chat message. Browser APIs still cannot prove physical completion or cancellation; MS2.0 claims neither.
- All six Export Hub formats are now owner-validated and protected. The required shared-root review confirms that the canonical snapshot, central format metadata, single pharmacy-scoped card, history/status routing, safe download dispatch and zero-feed-message rule are implemented once and inherited by every format. Future common export behavior must change these shared roots; format-specific exceptions require documentation and focused regression coverage.
- The six-format Export Hub live-validation sequence is closed. Do not start another export-format test or revisit a passed format without new regression evidence.
- Notifications Quiet State is **OWNER-VALIDATED PASS** and regression-protected. Owner evidence confirms Home showed `0 unread`, `Quiet` and `No urgent alerts`; the separate Notifications workspace opened with `0 unread`, its quiet-state message, no alert card and no Operations composer; returning Home left the state unchanged and created no sale, payment, import, stock change or alert.
- The next isolated Notifications checkpoint is **Pending Review Unread State**. Create one unapproved Paste List review containing one clearly new test medicine, then verify Home and the separate Notifications workspace expose exactly one truthful unread `Medicine import needs approval` alert. Do not approve, save, mark read or create any sale/payment/stock change; close the draft after evidence so the protected 35-medicine catalog remains unchanged.
- Owner evidence after `b9558ff` did not reach the expected alert: `Review list` changed the card to its editable review state but did not refresh the deterministic notification projection, so Home and Notifications correctly remained at zero for stale state. The plan was aligned with intended behavior; the implementation lifecycle was incomplete. The shared review transition now persists the review, refreshes Notifications, renders and focuses through one root. Input-only Paste List cards are explicitly excluded from alerts. Paste List also reuses the existing `startVoiceCapture`, `openLightweightCamera("shelf_photo")` and shared photo-input lifecycles through compact Mic, Camera and Photo controls; recognized photo rows enter the same review transition. The checkpoint remains open pending fresh owner evidence.
- Notifications Pending Review Unread State is **OWNER-VALIDATED PASS** and regression-protected. Evidence after `c63f687` confirms the Paste List Mic inserted the requested medicine line, Review list produced one correct unapproved editable row, Home showed exactly one unread import-approval preview, Notifications showed exactly one local Learning alert with the correct title/note/action/status and no Operations composer, cancellation preserved the 35-medicine catalog, and clearing/reading restored the protected quiet state at zero unread.
- The shared NotificationCard renderer now promotes only linked actionable notifications to one compact primary action button and removes that action from the generic field grid. Informational notifications receive no button. The linked action routes to the existing pending review card; title, category, note, status, unread/mark-read behavior, deterministic generation and workspace isolation are unchanged.
- The next isolated checkpoint is **Notifications Review Import Action Routing**. Recreate one unapproved test import only as the prerequisite, confirm the compact `Review import` button, tap it once, and require navigation to the exact existing editable review without creating a second card or saving anything. Cancel the draft and verify the catalog remains 35 and Notifications returns quiet. Do not test inventory, expiry or payment-failure alerts yet.
- Owner evidence after `9afd4e4` confirms the compact `Review import` button is visually passed, cancellation preserves 35 medicines, and quiet state returns. Action routing itself remains unpassed because the owner returned Home before tapping `Review list`, so the card was still `paste_input` raw text and no review or notification existed to open. Repository architecture explicitly defines `createPasteImportCard()` without prepared text as `paste_input`; `Review list` performs local parsing and changes that same card to `review`. Only this review-ready state is a valid pending approval target. Therefore the implementation is correct and the prior owner wording `Create one unapproved review` was ambiguous instruction drift, not owner error. The corrected retest names the required button and screen transition explicitly.
- Focused lifecycle verification now protects: raw input/voice creates no notification; `Review list` creates one review-ready draft and one unread notification; repeated projection does not duplicate either; `Review import` retains the same draft ID; no catalog save occurs; cancellation removes the generated notification and preserves 35 records; quiet state returns; actionable cards retain compact buttons while informational cards receive none.
- Notifications Review Import Action Routing is **OWNER-VALIDATED PASS** and regression-protected. Evidence confirms one tap on `Review list` created the editable `Notification Action Retest` draft and exactly one unread alert; the compact `Review import` action reopened that same draft without duplication; cancellation removed the alert; the catalog remained `Showing 35 of 35`; and Home returned to `0`, `Quiet`, `No urgent alerts`. The accepted lifecycle is raw Paste/Voice → `Review list` → editable review plus linked notification → `Review import` reopens the same review → Cancel/Approve clears the notification.
- The next isolated Notifications checkpoint is **Low-Stock Alert**. Use one existing saved medicine, record its current stock, change only Current stock to `5`, and verify exactly one unread local Inventory alert with the canonical low-stock title and count. Restore the recorded stock immediately afterward and require the protected 35-medicine catalog and quiet state. Do not test out-of-stock, expiry, payment failure, imports, sales or exports in this checkpoint.
- Notifications Low-Stock Alert is **OWNER-VALIDATED PASS** and regression-protected. Authoritative evidence records Cefixime at restoration baseline `22`, one approved change to `5`, immediate Catalog consistency, exactly one unread local Inventory alert titled `Cefixime is low` with note `Cefixime has 5 left. Prepare a restock if needed.`, and automatic removal after restoring `22`. Catalog stayed 35 and Home/Notifications returned to the protected quiet state.
- Notification reconciliation uses deterministic IDs and content fingerprints. Repeated projection cannot duplicate an alert or unread count; unchanged refresh preserves read state; a materially changed source fact refreshes the same alert and makes it unread; clearing the source condition removes it. Focused regression tests cover repeated refresh, changed low-stock counts and restoration.
- Catalog Medicine Action Cards reuse the existing shared microphone capture controller. Tapping a field then Mic, or speaking an explicit field phrase, updates the same local edit draft as typing; spoken numbers including zero and compound values are parsed locally. Existing validation, change tracking, approval, persistence and notification refresh are unchanged.
- Notifications Out-of-Stock Alert via Catalog Mic is **OWNER-VALIDATED PASS**, frozen and regression-protected. The authoritative 22-image sequence proves synchronized deployment, initial quiet state, 35 medicines, Cefixime baseline `22`, shared-Mic change to `0`, exactly one changed field, approval and persistence, the exact single unread Inventory alert through refresh without duplication, shared-Mic restoration to `22`, a second one-field review, 35-medicine preservation, alert removal and final quiet Notifications. Do not repeat the zero/notification/restoration workflow unless a future change directly modifies it.
- Catalog Search Mic is **OWNER-VALIDATED PASS / FROZEN / PROTECTED**. The authoritative nine-image sequence proves synchronized deployment, a visible Mic beside Catalog Search, truthful requesting/listening feedback, safe local zero-match handling for spoken `Sophie mix`, exact unique matches for spoken `Paracetamol` and `Ibuprofen`, clearing to `Showing 35 of 35`, accurate repeated Paracetamol use, and final Home `0`, `Quiet`, `No urgent alerts`. Different medicine names from the written examples are valid because the checkpoint tests the shared workflow, not fixed vocabulary. No sale, approval, stock mutation, notification, import, API or AI workflow occurred. Do not schedule it again unless future code changes the Search Mic feature.
- Notifications Expiry Alert Lifecycle is **OWNER-VALIDATED PASS / FROZEN / PROTECTED**. The authoritative 35-image sequence proves synchronized deployment; initial Quiet; 35 medicines; Catalog Search Mic selection of Ibuprofen; protected stock `27`, batch `IBU-200C` and expiry `2028-12`; shared-Mic expiry-only normalization to `2026-06`; exactly one changed field; approval and persistence; exactly one unread local Expiry alert titled `Ibuprofen has expired` with note `Batch IBU-200C expires at end of June 2026.` through refresh; shared-Mic restoration to `2028-12`; and final Home/Notifications Quiet. No unrelated workflow occurred. Do not schedule it again unless its implementation changes.
- **Main Operations Chat Activity Compaction — OWNER-VALIDATED PASS / PROTECTED**. Owner evidence confirms the Ibuprofen alias-only update and restoration produced one replaceable compact Activity card, no permanent full Catalog-update message, and two newest-first Activity History entries while the final alias baseline returned blank and Notifications stayed Quiet.
- **Shared Editable-Card Voice Viewport / Focus Preservation — OWNER-VALIDATED PASS / FROZEN / PROTECTED** on 2026-07-29. Final owner screenshots after `67cfcac` prove upper Strength `10 mg → 5 mg`, middle-field listening, lower Buying price `120 → 121`, and Expiry `2028-09 → 2026 May → 2028-09` all retained the selected field and medicine context in view; each review changed only one field and discard restored baseline. Earlier failures after `09cb128` and `b7ba547` remain historical evidence. Contextual sessions retain render/scroll ownership until discard or approval; do not regress or repeat without direct evidence.
- **Post-Validation User-Facing Product-Secrecy and Quiet-UI Audit — PENDING**. Preserve it as a later repository-backed whole-system checkpoint; do not mix it into the viewport repair.
- CODEX BRIDGE reports default permanently to five compact lines: Repository, Checkpoint, Files changed, Commit and Next owner action. Routine implementation/live-test reports use repository, checkpoint, at most three concise change bullets, tests, commit/push and exact owner action. Extra narrative is allowed only for errors or explicit owner request.

## Protected checkpoint registry

- `notifications-out-of-stock-via-catalog-mic`: **OWNER-VALIDATED PASS / FROZEN / PROTECTED** on 2026-07-28.
- `catalog-search-mic`: **OWNER-VALIDATED PASS / FROZEN / PROTECTED** on 2026-07-28.
- `notifications-expiry-alert-lifecycle`: **OWNER-VALIDATED PASS / FROZEN / PROTECTED** on 2026-07-28.
- `main-operations-chat-activity-compaction`: **OWNER-VALIDATED PASS / FROZEN / PROTECTED** on 2026-07-28.
- `shared-editable-card-voice-viewport-focus`: **OWNER-VALIDATED PASS / FROZEN / PROTECTED**.
- `post-validation-product-secrecy-quiet-ui-audit`: **PENDING**.

## Canonical execution pointer

MS2-LT-013 is closed and protected. The master owns the current execution pointer; do not initiate another checkpoint without an explicit owner command.

Next after owner PASS: master checkpoint **#54 Payment failure/cancellation notification**.

All remaining order, counts, prerequisites and classifications are maintained only in the master sequence.

## Protected validation history

- Owner screenshots record Cefixime `22 → 0 → 22` exclusively through the Medicine Action Card Mic, one-field reviews, exactly one canonical out-of-stock alert across refresh, complete restoration, 35 medicines and final `0`, `Quiet`, `No urgent alerts`.
- Owner screenshots record Catalog Search Mic permission/listening, invalid spoken-query safety, exact local Paracetamol and Ibuprofen matching, clear-to-35 behavior, repeated use and final Quiet Home without mutation.
- Owner screenshots record Ibuprofen expiry `2028-12 → 2026-06 → 2028-12`, stock `27`, batch `IBU-200C`, two expiry-only reviews, exactly one canonical Expiry alert through refresh, 35 medicines and final Quiet restoration.
- Do not cosmetically polish passed formats without genuine regression evidence.

## Preserved state

- Authoritative repository root: the current `pharmareen` checkout at `C:\Users\Pal\Documents\New project\.pharmareen_github_merge`.
- Active branch and remote: `main` tracking `origin/main` at `https://github.com/pal895/pharmareen`.
- Historical or duplicate checkouts, including `pharmareen (1)`, are non-authoritative and must not be modified. Stop before implementation if the repository root, branch, remote, or current checkpoint cannot be proven.
- Latest completed checkpoint: `5caac52` (`Close Zinc voice sale no mutation check`), pushed to `origin/main`.
- Catalog baseline: 35 medicines.
- Current stage: Mic Test 2 is complete. Broader daily-sales validation is next, beginning with the simulator-first Transaction Completion Engine foundation and one controlled Fast Record sale.
- Test 5 XLSX onboarding is complete and protected. Do not repeat it without new regression evidence.
- Accepted testing-path improvement: typed input is a permanent supported interaction lane, equivalent intents across typed, microphone, camera, photo, shelf photo, invoice, file, barcode, Paste List, and other supported sources must converge on the same shared domain behavior, and every active feature/entry point must receive efficient source-specific live coverage. This does not replace or advance Mic Test 2.

## Completed and protected live behavior

- Two invoice onboarding fixtures passed review, correction, approval, persistence, and export; invoice row ordering is protected.
- Clean bulk-paste onboarding passed and must not be repeated without regression evidence.
- CSV imported four medicines and increased the saved catalog from 21 to 25 without duplication. Strength, quantity, prices, supplier, batch, and optional blank barcode reached the catalog and Medicine Action Card.
- `SHOW ME`, complete catalog browsing, strict multi-term search, filtered `Open & edit`, draft-safe Medicine Action Cards, basic Cefixime sale, and basic Cefixime restock are live-approved.
- Test 5 XLSX approval and persistence are live-approved: the catalog grew exactly from 32 to 35, and Cetirizine, Co-Amoxiclav, and Paracetamol each persisted once with all reviewed values intact and no pending edit on reopen.
- The repeat Test 5 XLSX no-new-medicines result is live-approved end to end: `Open catalog` rendered the complete persisted 35-item catalog immediately, and closing returned to the unchanged chat without creating a message, draft, approval, sale, new medicine, or catalog mutation.
- Mic Test 2 voice navigation is live-approved on mobile: recognized `show me` opened the complete 35-item catalog, repeated safely, closed back to chat, and used zero OpenAI API tokens and requests.
- Mic Test 2 voice restock, generic-only safety, startup readiness, spoken-number parsing, normal-pronunciation sale review, bad-pronunciation sale review, cancellation and no-mutation behavior are live-approved.

## Current unresolved validation

- Deploy and live-confirm canonical month expiry handling removes the four false alerts produced from values such as `Oct-28`.
- Confirm the synchronized bottom catalog search added at `937cc3f`.
- Confirm shared row movement appears in a safe CSV or bulk-paste review and moves one intact row. Do not approve prepared existing data or create duplicates during this check.
- Live evidence after `6476f7c` verified false expiry alerts were removed and the synchronized bottom catalog search appeared. File and Paste List quick actions then exposed a shared interaction-lifecycle failure before a review could open.
- Live evidence after `60b2014` showed four import-approval alerts: repeated Paste List taps created empty drafts but did not navigate to the new card, making Operations appear hung while Notifications remained available. Empty repeated drafts now consolidate safely and every opened card is focused immediately.
- Live evidence after `b011b6b` showed the retained blank draft still blocked Operations after its notification was dismissed. Resume now discards every truly empty Paste input draft and isolates any unreadable non-empty card so operational workspaces cannot be locked by one draft.
- Live evidence after `481835d` verified MS2.0 Assistant and SHOW ME open again. Two valid import reviews reached isolation cards because of a renderer defect, and the bottom close label wrapped poorly; unreadable raw drafts now move to bounded local quarantine on resume and every shared bottom close control displays only `x`.
- Live evidence after `91a8d7e` verified the File picker opens, but selecting the Test 4 CSV produced an isolation card. Exact reproduction found a stale `incompleteInvoice` action-template reference left behind by the capability migration; completed CSV/Paste reviews now consume `correctionAllowed` from the shared policy and have full render regression coverage.
- Live evidence after `e4da9fa` verified the four-row CSV review renders and shared row movement preserves complete rows. A clean Paste List containing four already-saved medicines correctly created no duplicates, but its result was hidden in collapsed diagnostics; the shared CatalogImport note now exposes safe no-op review outcomes and the paste editor uses an owner-facing, full-width medicine-list field.
- Live evidence after `0eaa499` verified all-existing feedback and the improved paste editor. A mixed Paste List then correctly excluded saved Amitriptyline and reviewed only new Quinine Sulfate; the shared visible feedback now also explains this mixed partition instead of leaving the omitted existing row unexplained.
- Live evidence after `904c501` closed the mixed draft without saving, then the unknown-medicine sale command `Quinine Sulfate 1 cash` correctly opened a Medicine Match / sale-time learning review. Its shared renderer exposed every field as one long phone form and labeled both sale quantity and current stock as `Quantity`; shared single-medicine reviews now use three-slide progressive disclosure and distinct canonical labels. The Quinine draft remains unapproved.
- Live evidence after `d6027d6` verified all three compact panels and their navigation. The remaining raw `message` label and mobile textarea resize artifact are normalized at the shared field/style boundary; the Quinine draft remains unapproved.
- Live evidence after `3de02cb` verified local EAN-13 capture of the controlled Losartan fixture, Source Brain-gated recognition, a complete unsaved review, explicit approval, catalog growth from 25 to 26, and persistence after refresh. The saved record retained name, strength, form, unit, stock, prices, supplier, barcode, and expiry, but lost `LOS-50T`: shared review records exposed top-level `batch`/`expiry` while Pharmacy Brain normalization only retained a prebuilt `batches[]` array. Canonical normalization now promotes those reviewed traceability fields into `batches[]` for barcode, photo, manual, and other shared review approvals.
- Live evidence after `0e232c5` verified deployment and repeated Losartan recognition without creating a 27th medicine, but the repeat review displayed blank Quantity even though the saved catalog held stock 40. The repeat path read flat fixture-shaped fields directly from a canonical saved record, and sparse upserts could replace durable values with empty strings. Barcode matches now enter the shared canonical review normalizer, and shared catalog merging preserves meaningful saved stock, commercial, location, and traceability values when an incoming review is sparse.
- Live evidence after `e158ec9` verified the complete repeated Losartan review retained stock 40, buying price 15, selling price 25, supplier, packaging, barcode, batch `LOS-50T`, and expiry `2029-06`; the unapproved review left the Pharmacy Catalog at 26 medicines. The next isolated Source Brain fixture is Loperamide 2 mg capsule, EAN-13 `6161109876553`, prepared for new-medicine barcode coverage without overlapping the known live catalog.
- Live evidence after `0d7ce83` verified Loperamide recognition, approval, refresh persistence, and repeat-scan consistency across all trusted fields; cancellation left the catalog duplicate-safe at 27 medicines. The next prepared barcode fixture is valid EAN-13 `6161109876560` with intentionally no registry mapping, for verifying an honest unsaved unknown-barcode fallback without invented medicine data.
- Live evidence after `0ba0aaa` verified the unregistered EAN-13 decoded locally with only its barcode populated and no invented medicine data. The shared card still displayed generic confirmation guidance and an active Confirm button while medicine identity was blank. All progressive medicine reviews now inherit one identity-readiness gate in visible guidance, button state, and the confirmation action boundary.
- Live evidence after `0874b68` verified the unregistered barcode's clear missing-match message, disabled Confirm action, safe cancellation, and unchanged 27-item catalog. Barcode coverage is complete. The next prepared fixture is a realistic controlled B2 shelf photo containing Prednisolone and Septrin; its isolated filename mapping is Source Brain-gated and converges into the shared two-row catalog review without saving before approval.
- Live evidence after `53cea3a` verified the Shelf photo acquisition route and its safe unmatched fallback: the Android photo picker changed the selected fixture's filename, so the filename-only controlled-fixture boundary produced a blank, disabled review and saved nothing. Controlled shelf fixtures now carry a verified SHA-256 identity and photo-library/camera acquisition retains the actual File for content hashing; filename remains a fast path, unknown content still follows the safe normal review pipeline, and Source Brain/readiness gates remain mandatory.
- Live evidence after `a72ce33` showed Google Photos also re-encoded the selected image, changing its exact SHA-256 bytes; the review again stayed blank, disabled, and unsaved. Controlled fixture identity now adds a bounded visual fingerprint plus aspect-ratio check after the filename and exact-hash paths, tolerating harmless image encoding changes without turning on general shelf recognition or weakening the unknown-image safety boundary.
- Live evidence after `f670051` verified the re-encoded Google Photos selection resolved into one unsaved two-row shelf review. Prednisolone retained strength 5 mg, tablet form/unit, stock 24, buying 7, selling 12, supplier MedSource Kenya, batch `PRE-5T`, expiry `2028-11`, and shelf B2. Septrin retained suspension/bottle, stock 12, buying 120, selling 180, supplier MedSource Kenya, batch `SEP-100S`, expiry `2028-09`, and shelf B2. The visible boundary confirmed nothing was saved before approval; no unsupported strength or barcode was invented.
- Subsequent Shelf B2 evidence verified one approval increased the catalog from 27 to 29 and both saved records retained every reviewed meaningful field after reopening: Prednisolone stock 24, buying 7, selling 12, supplier MedSource Kenya Ltd, batch `PRE-5T`, expiry `2028-11`, shelf B2; Septrin stock 12, buying 120, selling 180, supplier MedSource Kenya Ltd, batch `SEP-100S`, expiry `2028-09`, shelf B2. Gallery shelf onboarding is complete and must not be repeated without regression evidence.
- Direct-camera Shelf Photo validation is queued next with the controlled C3 fixture containing canonical Metronidazole and Ibuprofen. Shelf photo now explicitly offers `Take photo` and `Choose from phone`; direct shelf capture uses the rear-camera lifecycle, pauses at a Retake/Use photo decision, then converges into the same local fixture recognition, Source Brain gate, canonical multi-row review, approval, and persistence path as gallery selection. Brufen remains an Ibuprofen alias rather than a competing canonical Source Brain identity.
- Live evidence after `40aa017` verified the new shelf acquisition chooser, rear camera, shelf guidance, Capture, preview, Retake, Use photo, and no-save safety in both sideways and upright phone use. Recognition failed safely in both orientations because the whole-frame visual identity compared portrait captures—including screen framing, margins, and rotation—against a landscape fixture. Shared fixture normalization now evaluates all four right-angle luminance fingerprints and accepts reciprocal portrait/landscape aspect ratios within the same isolated content gate. The acquisition sheet also uses content-height controls instead of stretching its whitespace and three buttons across the full phone height.

## Shared-root conclusions and drift

- Permanent trusted-result rule: every approved result is a baseline. Later workflows and UX improvements must preserve its meaningful canonical facts unless the owner explicitly changes them. Empty, absent, placeholder, or failed-extraction values never erase trusted catalog data. Apply the checklist and fast `npm run verify:consistency` gate documented in `trusted-result-consistency.md` before every relevant commit.

- Medicine identity fields and persistence converge through the canonical medicine field schema; strength and barcode are not CSV-only.
- Reviewed batch and expiry values must converge into canonical `batches[]` during Pharmacy Brain normalization even when the originating shared card supplies flat fields.
- Every saved-catalog record reopened in a review must pass through canonical field normalization; never manually read only one storage shape. Sparse approvals must not erase durable pharmacy values, while explicit catalog edits remain the owner-controlled way to clear or replace them.
- Photo acquisition must support both direct camera capture and existing-photo selection where appropriate. Acquisition controls may differ, but acquired files must converge into one recognition, normalization, editable review, duplicate prevention, approval, and persistence root. Never assume a new pharmacy already has the required image stored, and live-validate camera and gallery acquisition separately.
- Codex prepares realistic, non-overlapping controlled fixtures and exact expected metadata before each onboarding test. Completed recognition stages are not repeated without regression evidence.
- Multi-row review movement was generic in persistence but its UI was incorrectly gated by invoice mode. The shared CatalogImport review policy now owns reorder, add-row, editing, approval, and correction capabilities.
- All `data-action` controls now delegate through one stable application-root lifecycle. Every card inherits the same safe close action at both the top and bottom so long editable cards do not require a full upward scroll.
- Safe invoice, CSV, bulk-paste, and future compatible multi-row CatalogImport reviews inherit ordering. Incomplete invoices intentionally hide ordering and add-row controls until safe; single-medicine cards exclude ordering.
- Context parsers may differ, but supported fields must converge before editable review. Excel parsing and genuine barcode decoding remain future stages, not part of this fix.

## Historical pre-closure live-test sequence — superseded

The sequence below records the earlier onboarding-to-operations path and is retained as provenance. Its cases were subsequently completed and protected or replaced by the reconciled authoritative sequence above. It must not be interpreted as a second current queue.

1. Finish the combined CSV-stage validation: false alerts removed, bottom search present, and one shared row move works without approval.
2. Validate Excel onboarding only after its adapter is ready.
3. Cover remaining non-clean paste variants, then manual creation and sale-time learning.
4. Validate barcode/shelf/photo onboarding, then broader Medicine Action Card cases.
5. Run Mic Test 2 before broader sales and restocking coverage.
6. Validate the in-app Export Hub and reports/export integrity, then the full Notifications stage once catalog dates are trustworthy. The owner must generate and download real supported Excel, PDF, Word, CSV, and future document formats from MS2.0 itself; Codex workspace fixture creation is not production proof.
7. Continue to Digital Operations Assistant and Operational Intelligence.

Do not repeat the two passed invoice fixtures, clean paste fixture, filtered catalog action, strict Zinc search, basic Cefixime sale, or basic Cefixime restock unless new regression evidence appears.

## Permanent reusable-behavior rule

Whenever an editable-medicine behavior is reusable, implement it once in the shared editable-card or editable-list capability and make every applicable workflow inherit it. Preserve one canonical implementation and focused regression protection. Permit an exception only when a context-specific safety or workflow reason is documented and tested; never duplicate shared behavior screen by screen.

## Permanent repository asset rule

The Git repository is the authoritative shared source for important development and validation assets. Whenever appropriate, commit fixtures, Excel and CSV samples, PDF and Word templates, export examples, onboarding samples, testing assets, downloadable reports, and other generated documents needed to reproduce or review a workflow. Do not leave an important downloadable asset only in a Codex workspace, temporary folder, local download directory, or unshared runtime path. Runtime-owned secrets, private pharmacy data, caches, and disposable generated output remain excluded. This rule does not change the active live checkpoint or turn a committed fixture into proof of an in-app production download.

## Permanent live-test execution rule

Every live-test response must be self-contained and include the exact executable command, exact fixture or route, ordered owner actions, approval boundary, expected result, preserved catalog baseline, and requested next evidence. Use the stable response structure and pre-send completeness check in `live-test-execution-discipline.md`. Focused cycles begin with this current state, the latest relevant diff, and directly affected shared components; they use focused tests plus only necessary protected regressions. Repository-wide rediscovery, repeated architecture analysis, unrelated broad suites, implementation loops, and regeneration of known context are prohibited. Local-first, deterministic-first, zero-token-first, and all existing API-token preservation rules remain mandatory. This process rule does not advance or replace the active checkpoint.

Every future live-test checkpoint must end with one complete owner package containing exactly: Objective; Expected behaviour; numbered exact owner steps; the expected result after every step; Required screenshots; PASS criteria; and FAIL criteria. Never provide only the checkpoint name. Automated verification, repository inspection or earlier evidence cannot be relabeled as owner validation.

Permanent circuit breaker: after the root cause is identified, if two further inspection or command cycles produce no new evidence, stop expanding the investigation. Implement the smallest justified shared-root fix or report one concrete blocker; never continue an open-ended loop.

## Permanent same-commit Project Brain rule

The repository is the MS2.0 Project Brain. When an accepted change affects architecture, permanent behavior, UX decisions, protected behavior, fixtures, token policy, testing sequence, or the active checkpoint, update the relevant Project Brain and Engineering Memory documents in the same commit as the implementation and focused regression protection. The task is not complete until implementation and focused tests pass, the current live-validation state is accurate, and the commit is pushed. Trivial implementation details that do not change permanent project knowledge do not require documentation churn.

Before modifying MS2.0, a fresh Codex chat must verify the repository root, working directory, branch, remote, latest relevant commit, authoritative Project Brain files, and active checkpoint with safe read-only commands. If repository evidence conflicts with a handoff, stop and report the discrepancy instead of guessing. Synchronization itself performs no implementation.

## Permanent honesty and simple-language rule

The product-wide standard in `honesty-and-simple-language.md` is mandatory for all current and future MS2.0 work. Every important value must retain an honest source; uncertainty must be visible; unsupported facts must remain empty; and all owner-facing text must use short, everyday English understandable by a 12-year-old. Apply the rule through shared capabilities and implementation review, not as isolated screen fixes.

## Shelf-photo trust and first-response rule

Live C3 evidence verified successful recognition in both sideways and upright phone use, but one sideways first attempt opened an empty review before a repeat produced the correct two-row result. A shelf photo must now produce one of only two honest responses: a recognized review, or a clear Retake instruction that leaves the preview open. It must never open an empty approval form.

Controlled fixture reviews must disclose field sources. The C3 image visibly supports medicine names, strengths, tablet form, and shelf C3. Stock, prices, supplier, batch, and expiry come from the prepared controlled-test record and are not read from the photo; barcode remains blank. Real unknown photos must not receive prepared fixture values. Camera guidance must use simple words suitable for a 12-year-old, such as “keep bright light off the packs or screen,” rather than “glare” or “reflections.”

Live evidence after `f33d434` verified the safe first-response rule. A sideways C3 capture stayed in the preview with a clear Retake message, opened no empty review, and saved nothing. A later upright capture opened the correct unsaved Metronidazole and Ibuprofen review with the honest controlled-test source message and blank barcodes. The owner found that sideways capture remained less reliable. The screenshots also showed that the failed preview still offered `Use photo` after instructing the owner to Retake, and the important source message appeared only after scrolling past both long medicine rows.

Shelf camera guidance now recommends holding the phone upright while sideways capture remains supported. Controlled matching evaluates the full image and two small symmetric crops through all four right-angle orientations, reducing the effect of camera borders and framing without weakening the isolated registry, aspect-ratio boundary, Source Brain gate, review gate, or approval boundary. A failed read now offers Retake only. Important multi-row review feedback, including shelf-photo provenance, appears before the editable rows so the owner sees the source before reviewing values.

Live evidence after `cf3aab3` closed the C3 sideways recognition and review-readiness checkpoint. Replit pulled and started successfully, the shelf camera showed the upright-phone advice, and one sideways capture opened the correct unsaved two-row Metronidazole and Ibuprofen review. The controlled-test source message appeared before the rows; medicine names, strengths, tablet form, and shelf C3 were correctly identified as photo evidence; prepared stock, prices, supplier, batch, and expiry were disclosed separately; and both barcodes remained blank. No new friction was visible and no approval occurred.

The subsequent C3 approval and refresh evidence passed. One approval grew the live Pharmacy Catalog from 29 to 31. After refresh, catalog search found one Metronidazole and one Ibuprofen record. Metronidazole retained strength 400 mg, tablet form and unit, stock 36, selling price 20, buying price 11, supplier Afya Wholesale Ltd, shelf C3, blank barcode, batch `MET-400C`, and expiry `2029-03`. Ibuprofen retained strength 200 mg, tablet form and unit, stock 28, selling price 18, buying price 9, supplier Afya Wholesale Ltd, shelf C3, blank barcode, batch `IBU-200C`, and expiry `2028-12`. Opening either saved record produced an unchanged draft with `Approve & save` disabled, confirming that inspection alone did not create a durable edit.

The final repeated C3 approval passed duplicate prevention: the catalog remained at 31 with one Metronidazole and one Ibuprofen, and both records retained the trusted values above. The same live sequence exposed a camera-composition regression. Adding mandatory upright guidance to the first attempt made the instruction longer, and the camera panel had only a maximum height, so the black viewing area became shorter. Owners may reasonably choose sideways capture for a wide source. The first attempt must therefore stay orientation-neutral; upright is a retry suggestion only after a failed sideways read. Active camera panels now use the full available viewport height while keeping `object-fit: contain`, so the whole source remains visible inside a stable black viewing area without cropping. The compact acquisition chooser remains content-height.

Live evidence after `ed668fe` closed the camera-composition regression and the full C3 shelf-photo stage. Replit pulled and restarted successfully. The first camera guidance was orientation-neutral, the large black viewing area was restored, and the complete C3 source fit without cropping in both upright and sideways captures. Both previews retained Cancel, Retake, and Use photo controls and clearly stated that nothing had been saved. No approval occurred during this final presentation check, so the trusted catalog baseline remains 31. C3 now has live passes for acquisition, camera lifecycle, orientation handling, honest recognition or Retake behavior, visible provenance, approval, refresh persistence, complete field retention, and duplicate prevention. Do not repeat C3 without new regression evidence.

## Medicine-photo onboarding stage

The existing generic medicine-photo route was adapter-ready scaffolding: every photo produced an empty correction card and direct camera capture skipped the Retake/Use photo decision. Full medicine-photo onboarding now begins with one isolated Amoxicillin 500 mg capsule fixture that does not overlap the trusted 31-medicine catalog. Amoxicillin is reusable Source Brain knowledge only; it is not pharmacy-owned data until explicit approval. The realistic controlled carton visibly contains name, strength, capsule form, 20-capsule pack size, valid EAN-13 `6161109876577`, batch `AMX-500K`, and expiry `2029-08`. Stock, buying price, selling price, supplier, and shelf are unsupported by the image and remain empty.

Shelf and medicine controlled photos share one local filename, SHA-256, bounded crop-aware perceptual identity, and orientation-normalization root. Direct medicine capture now pauses at Retake/Use photo like shelf capture. A recognized fixture enters the shared single-medicine review with visible field provenance, Source Brain gating, owner review, and no save before approval. An unclear direct capture stays in preview with a simple Retake-only response; an unclear gallery photo opens no empty approval card. Production behavior remains isolated from controlled fixture metadata, and ordinary processing remains local and zero-token.

Initial live medicine-photo evidence after `f0c6fa8` verified the full camera and preview lifecycle plus safe Retake behavior, but several visibly clear upright and sideways Amoxicillin captures did not match. Every failed result remained in preview, removed `Use photo`, opened no empty card, and saved nothing. The failure was not owner framing or unreadable text: name, strength, form, pack size, batch, expiry, and barcode were visibly clear. The generated fixture's whole-frame fingerprint did not represent the screen borders, perspective, and orientation present in real camera captures. Controlled image registries now share one identity matcher that accepts a bounded list of verified visual references while retaining filename, SHA-256, aspect-ratio, orientation, Source Brain, review, and approval boundaries. The verified upright and sideways Amoxicillin camera references are isolated fixture evidence, not production medicine truth.

## Next action

Live evidence after `3478fc5` verified the complete upright medicine-photo path. One clear Amoxicillin capture opened an honest unsaved review: name, strength, capsule form, pack size, barcode, batch, and expiry came from the photo; stock, prices, supplier, and shelf remained blank. No approval occurred and the Pharmacy Catalog remained 31. The same sequence isolated a remaining orientation regression: two clear sideways captures failed safely, but their retry message omitted the agreed upright alternative. These captures used newer close and offset camera compositions that were outside the bounded verified reference list.

The controlled medicine registry now includes those two verified sideways compositions without increasing the visual tolerance or weakening the aspect-ratio, Source Brain, review, or approval gates. Initial guidance remains orientation-neutral because sideways is supported. Only after a failed read does the retry message say: `If the phone was sideways, try holding it upright.`

Live evidence after `a47ca1c` closed the medicine-photo orientation checkpoint. Replit pulled and restarted successfully. One sideways Amoxicillin capture retained the whole pack inside the full black viewing area and opened one honest unsaved review. The source disclosure appeared before the fields. Name Amoxicillin, strength 500 mg, capsule form and unit, pack size 20 capsules, barcode `6161109876577`, batch `AMX-500K`, and expiry `2029-08` were correct. Selling price, buying price, quantity/stock, supplier, shelf, and category remained blank because they were not visible. No approval occurred. Sideways and upright medicine capture now both have live recognition passes; a failed attempt retains the upright retry alternative.

Live evidence after `ec4517b` closed medicine-photo cancellation safety. After cancelling the unsaved Amoxicillin review, the complete saved Pharmacy Catalog still contained 31 medicines. Searching `Amoxicillin` returned zero results in both catalog search views. The controlled Source Brain match therefore did not become pharmacy-owned data without explicit approval.

Live evidence after `fdf569b` closed medicine-photo explicit approval and refresh persistence. The owner reviewed the same sideways Amoxicillin capture, kept unsupported fields blank, and confirmed once. The app reported `Medicine details saved.` After refresh, the complete saved Pharmacy Catalog contained 32 medicines and search returned exactly one Amoxicillin. Opening that saved record retained 500 mg, capsule form and unit, pack size 20 capsules, barcode `6161109876577`, batch `AMX-500K`, and expiry `2029-08`. Current stock, selling price, buying price, supplier, shelf, reorder level, and aliases remained blank. The edit view reported `No changes yet` and left `Approve & save` disabled, so inspection alone created no durable edit.

Live evidence after `e1b3548` closed medicine-photo duplicate prevention and the complete Amoxicillin onboarding stage. A second capture and confirmation produced another `Medicine details saved.` acknowledgement, but after refresh the complete catalog still contained 32 medicines rather than 33. Exactly one Amoxicillin remained. Its 500 mg strength, capsule form and unit, 20-capsule pack, barcode `6161109876577`, batch `AMX-500K`, and expiry `2029-08` were unchanged. Stock, prices, supplier, shelf, reorder level, and aliases remained blank. Opening the record again showed `No changes yet` and kept `Approve & save` disabled.

Medicine-photo onboarding now has live passes for direct acquisition, full-frame preview, Retake/Use photo lifecycle, upright and sideways recognition, failure fallback, visible provenance, supported-field honesty, cancellation safety, explicit approval, refresh persistence, complete field retention, and duplicate prevention. The trusted Pharmacy Catalog baseline is now 32. Do not repeat the Amoxicillin fixture without new regression evidence. Await the owner's next feature or validation target before expanding scope.

## Excel onboarding stage

Repository synchronization after `9959cc2` confirmed that invoice onboarding, clean Paste List, CSV, barcode, gallery/direct shelf photo, and medicine photo are closed. The ordered validation plan names Excel as the first unvalidated acquisition path, but its live route was still an explicit placeholder: `.xlsx` and `.xls` opened an `ImportMappingCard` saying binary parsing was reserved. Excel therefore required adapter preparation before any honest live test.

Modern `.xlsx` onboarding now uses a small local OOXML reader that extracts the first worksheet, supports standard stored or deflate-compressed workbook entries, and sends the resulting rows through the existing delimited inventory parser, Source Brain match, canonical field normalization, shared editable multi-row review, duplicate prevention, approval, and persistence path. No AI, backend extraction, or token use is introduced. Older binary `.xls` remains unsupported and now fails visibly with a simple save-as-XLSX-or-CSV instruction and an explicit no-save statement.

The controlled Test 5 workbook contains three Source Brain medicines absent from the trusted 32-item catalog: Cetirizine 10 mg, Co-amoxiclav 625 mg, and Paracetamol 500 mg. It includes form/unit, prices, stock, supplier, batch, canonical month expiry, pack size, and shelf; barcode is intentionally blank. The first live checkpoint is upload and unsaved review only. Expected catalog count remains 32 until explicit approval.

Live evidence after `7a6335c` verified Android file acquisition, local XLSX extraction, and one correct unsaved three-row review. Cetirizine, Co-amoxiclav, and Paracetamol retained every prepared supported value in source order, and all intentionally unsupported barcodes remained blank. No approval occurred, so the trusted catalog remains 32. The review exposed one honesty friction: its shared constructor still labeled every file acquisition as `Bulk paste` and showed only generic review guidance, so the owner could not see that the values came from the selected workbook. File imports now preserve the selected filename and file type at the shared CatalogImport boundary and display the number of medicines read, any rows omitted as unreadable, and the no-save-before-approval boundary. Paste List retains its existing behavior. Repeat only this unsaved XLSX review checkpoint to confirm the visible Excel provenance; do not approve or advance yet.

Live evidence after `772dc1f` closed the corrected XLSX unsaved-review checkpoint. Replit pulled through `0200554` and restarted with backend health OK. The review visibly stated that three medicines were read from Excel file `test-5-excel-import.xlsx`, instructed the owner to check every value, and stated that nothing was saved until approval. Cetirizine 10 mg remained row 1 with 30 tablets, stock 45, buying 8, selling 15, EastCare Pharma, batch `CET-10A`, expiry `2029-04`, shelf D1, and blank barcode. Co-Amoxiclav 625 mg remained row 2 with 14 tablets, stock 24, buying 60, selling 85, EastCare Pharma, batch `COA-625B`, expiry `2028-11`, shelf D2, and blank barcode. Paracetamol 500 mg remained row 3 with 100 tablets, stock 100, buying 5, selling 10, EastCare Pharma, batch `PAR-500C`, expiry `2029-06`, shelf D3, and blank barcode. No unreadable-row warning appeared because no rows were omitted. No application friction was visible and no approval occurred, so the trusted catalog remains 32. The next Excel checkpoint is one explicit approval followed by refresh and catalog verification at 35 medicines; do not re-upload the workbook before that approval.
- The XLSX persistence sequence exposed a navigation collision: typing `Show me` created a false Medicine Check instead of opening the catalog, while the home `SHOW ME` tile worked. Catalog-navigation phrases are now intercepted locally, case-insensitively, before medicine/list parsing for typed and voice input. Live-confirm this routing fix without approving or saving anything; the catalog must remain 35.

Live evidence after `d57b24a` closed the typed catalog-navigation collision. Replit pulled and restarted successfully, and natural-case typed `Show me` opened the complete 35-medicine Pharmacy Catalog directly. No Medicine Check appeared, no review or save action occurred, and the trusted catalog baseline remained 35. This routing checkpoint is passed and must not be repeated without regression evidence.

The permanent catalog entry is now also a compact, accessible catalog icon in the main chat header. It is outside the message history and shares one `navigateToCatalogWorkspace` controller with the home SHOW ME tile, typed and voice catalog phrases, and catalog-card entry. Tapping the icon must not create a message, review card, draft, or mutation. The next authoritative Excel checkpoint remains repeat-import duplicate prevention: reselect the approved Test 5 XLSX and verify that all three existing rows are safely omitted and the catalog remains exactly 35. The same live sequence must validate the new header icon before that repeat import; this does not create a separate detour or advance beyond Excel onboarding.

Live evidence verified the compact header catalog icon: it opened the complete 35-medicine catalog without a message, draft, or mutation. The subsequent repeat Test 5 XLSX selection exposed a duplicate-prevention presentation gap. The file route created a normal editable three-row approval review even though all three medicines were already saved. The owner did not approve it, so the catalog baseline remains 35. Root cause: Paste List partitioned against the saved catalog before review, while CSV/XLSX file acquisition bypassed that shared decision and constructed the review directly.

All list-based catalog acquisition now uses one `prepareCatalogImport` boundary. It partitions incoming rows against the current Pharmacy Catalog before any editable review is created. An all-existing XLSX/CSV/TSV file produces a non-approvable no-changes result that names the existing medicines, states that nothing was saved, and offers Open catalog, Read, or Close. A mixed file exposes only genuinely new rows and discloses which existing medicines were not added again. The active checkpoint has not advanced: repeat the same committed Test 5 XLSX, verify the no-new result, then open the catalog and confirm it remains exactly 35. Do not approve anything.

Live evidence confirmed the repeat Test 5 XLSX is safely non-approvable: it named all three existing medicines, stated that nothing was saved, and preserved the 35-item catalog. The remaining checkpoint defect is isolated to the result card's `Open catalog` action. The card already called the shared `navigateToCatalogWorkspace` controller, but its direct action branch omitted the render performed by the header and typed-command routes. The branch now renders immediately after shared navigation. Retest only this button; the catalog must open at 35 with no message, draft, approval, or mutation.

Live evidence after `114e66d` closed the final Excel checkpoint. Replit pulled through `b399118`, restarted with backend health OK, and the repeated `test-5-excel-import.xlsx` produced the expected non-approvable result naming Cetirizine, Co-Amoxiclav, and Paracetamol and stating that nothing was saved. Tapping `Open catalog` immediately displayed the complete persisted Pharmacy Catalog with `Showing 35 of 35`. Closing returned cleanly to the same chat history. No new chat message, draft, approval request, sale, medicine, or catalog mutation appeared. Modern XLSX onboarding, persistence, duplicate prevention, and all shared catalog-opening entry paths are now complete and protected.

The next authoritative checkpoint is Mic Test 2 before broader sales and restocking. Begin with one safe voice-navigation pass: on the same online mobile device, tap `Mic`, allow microphone access if requested, clearly say `Show me`, and confirm recognized speech uses the shared local catalog-navigation controller to open the complete 35-item Pharmacy Catalog. Close it and confirm the unchanged chat returns with no Medicine Check, draft, approval, sale, new medicine, or catalog mutation. This first checkpoint approves or saves nothing and establishes the live microphone-permission, listening, recognized-text, shared-route, mobile, and zero-mutation boundary before voice sale or editable-card coverage.

Live evidence after `d9feb8c` closed the first Mic Test 2 checkpoint. Replit pulled and restarted with backend health OK. On the mobile app, spoken `show me` was recognized twice on intentional repeat, and each attempt opened the complete persisted Pharmacy Catalog with `Showing 35 of 35`. Closing returned to the same chat, where only the two intentional recognized owner entries were added. OpenAI Platform evidence before and after showed zero tokens, zero requests, and zero spend. No Medicine Check, approval, sale, new medicine, or catalog mutation occurred. Repeated safe voice catalog navigation is now protected.

The next Mic Test 2 checkpoint is one unsaved shared-matcher voice review. Tap `Mic`, say `Restock zinc sirup`, and verify the recognized transcript opens an editable `Check voice result` card whose medicine is canonically resolved to `Zinc` despite the spoken form spelling variation. Do not approve or save it. Close the card and confirm no stock, catalog, sale, approval, or API-token change occurred. This is the smallest controlled proof that speech-recognized text reaches the shared local medicine matcher and editable review boundary before any voice mutation is tested.

Live Mic Test 2 evidence exposed three shared frictions. When Wi-Fi dropped, browser speech failed but MS2.0 incorrectly blamed microphone permission. One online attempt ended without a result or recovery message. A fast attempt was transcribed only as `syrup`; MS2.0 honestly preserved that partial transcript and saved nothing, but the generic fallback exposed a medicine-creation form. A slower attempt correctly produced `restock zinc syrup` and resolved canonical Zinc, but the voice boundary flattened the real Restock card into a sale-shaped `Check voice result` with an irrelevant Payment field and without the full restock workflow. The owner cancelled every draft; no stock or catalog mutation occurred.

Voice errors now distinguish offline, permission, network, no-speech, and other stopped-capture outcomes using short owner-facing guidance. Fast speech is never repaired by inventing omitted words: the visible transcript remains the truth, and the owner can retry or edit. Recognized restock speech now retains the shared `RestockCard` instead of becoming a sale review. A missing quantity remains blank and blocks `Add stock`; it no longer silently defaults to one. The reusable three-section review keeps the workflow within speak/type, review, and confirm while exposing medicine, stock to add, bonus stock, unit, pack size, strength/form, buying and selling prices, supplier, batch, expiry, barcode, shelf, invoice/delivery reference, and note. Known catalog facts are reused, unsupported facts remain blank, and purchased and bonus stock remain distinct. Focused voice-restock, medicine matcher, progressive-card, architecture, shared-field, consistency, and zero-token checks pass.

Retest only the repaired boundary. While offline, tap `Mic` and confirm MS2.0 says voice needs internet and typing still works. Reconnect, tap `Mic`, say `Restock Zinc syrup 12` at a normal quick speaking pace, and confirm the visible transcript contains the intended words and the `Check restock` card shows canonical Zinc, stock to add 12, the saved unit, no Payment field, and the three simple sections. Inspect the remaining optional delivery fields, then close without tapping `Add stock`. Confirm the chat returns, the catalog remains 35, Zinc stock is unchanged, and API usage remains zero.

The first live retest after `d6901b9` confirmed the new offline wording exactly: `Voice needs internet on this phone. You can type while offline.` The online sequence remains unresolved. Across two attempts the visible owner bubbles differed: one showed `zinc syrup 12`, while a later bubble showed `restock zinc syrup 12`. The visible review remained a generic `Check voice result` containing transcript `zinc syrup 12`, Medicine Zinc, Quantity 12, Payment cash, and Confirm/Correct/Cancel rather than the expected `Check restock` workflow. This may represent partial browser transcription, an older active card remaining visible, or a focus/state mismatch between consecutive voice attempts; do not guess which until the controlled mic continuation isolates it. The owner also reported that an unexpected phone voice plays after tapping Mic and beginning to speak. Its source is not yet proven; it may be browser/device speech-service feedback rather than MS2.0 audio. Record its exact words and timing before any implementation decision. No approval or mutation is evidenced.

Permanent cross-input consistency rule: when the same intent enters through typing, microphone, scanning, importing, photographing, selecting, or another supported source, preserve medicine identity, form, strength, quantity, unit, pricing, payment where relevant, supplier, bonus, discount, batch, expiry, approval state, mutation rules, and final canonical result. Source-specific UI may differ, but shared interpretation and persistence must not drift. Validate representative equivalence at the logical workflow stage instead of duplicating every test. A passed shared-root test reduces repetition but never substitutes for source-specific permission, acquisition, parsing, failure, or rendering coverage.

Permanent active-feature coverage rule: every discoverable active feature must eventually be mapped to its entry point, input source, shared processing root, source-specific responsibility, user-visible result, mutation/no-mutation, approval boundary, offline behavior, token expectation, regression protection, prior shared coverage, and smallest remaining live test. The current quick-action list is Camera, Photo library, Shelf photo, File, Invoice, Scan barcode, Paste list, Stock fix, Report, Export CSV, and Setup. Track shared-root equivalence separately from source-specific coverage so no feature is silently omitted and passed workflows are not inefficiently repeated.

The typed `restock zinc syrup` observation is pending future representative equivalence validation, not a pass or failure. At the appropriate typed/restock stage, compare it with the spoken intent through the earliest shared root and verify canonical Zinc identity, syrup form, strength where known, explicit quantity safety, unit/pack handling, supplier, prices, bonus, discount, batch, expiry, and approval behavior. Do not create a Zinc-only rule. Mic Test 2 remains the immediate checkpoint.

Follow-up evidence isolated the audible event as a short Android/Chrome recognition pop about two to three seconds after tapping Mic and again when the owner manually tapped Stop. It contains no spoken words. This is native browser/device speech-service feedback, not audio generated by MS2.0. Suppressing the native start sound would require replacing browser recognition with recorded-audio transcription; the only repository-backed alternative currently uses OpenAI transcription and would violate the active zero-token boundary. MS2.0 therefore must not pretend it can silence the first native pop.

The shared UX root is repaired instead. While listening, the Mic control is disabled and reads `Listening`; the owner sees `Speak now. Listening stops by itself.` and no longer needs to tap Stop, removing the second avoidable pop. Every recognized voice result is review-first, including otherwise complete sales, so a misheard medicine cannot mutate stock or sales instantly. A new voice result replaces the prior voice draft, preventing an older Loratadine or generic card from remaining as the apparent result. Every voice review displays the exact heard transcript prominently before its action fields. Focused voice-restock, medicine matcher, consistency, architecture, and zero-token verification pass.

Retest only this repaired Mic boundary. Tap Mic once, do not tap it again, say `Restock Zinc syrup 12`, and wait for listening to stop itself. Confirm there is at most the native start pop, the button never offers Stop, the exact heard words are visible on the new card, no older voice card remains, and the result is the complete `Check restock` review. Close without `Add stock`; catalog and Zinc stock remain unchanged.

Live evidence after `9e5fb8b` proved the remaining timing root. When the owner began speaking immediately after tapping Mic, Chrome had not started capturing audio; the native pop arrived two to three seconds later, `restock` was lost, and only `zinc syrup 12` reached MS2.0, correctly producing a review-first generic sale card. When the owner waited for the pop and then spoke, the full `restock zinc syrup 12` transcript reached MS2.0 and the correct complete Restock card appeared with the exact heard-text banner. The final screenshots also showed the long `Listening` button colliding visually with Send and a no-speech result when no words followed the ready state.

Voice acquisition now has two truthful phases. Immediately after tapping Mic, the compact button reads `Wait` and the hint says `Starting microphone… Please wait.` Only after Chrome fires its actual recognition/audio-start event does the button read `Speak` and the hint say `Speak now.` Both states are disabled and auto-stop, so the owner cannot create a second pop or overlap Send. This fixes the shared startup gap for fast speakers without buffering, guessing, AI transcription, or token use. The exact heard transcript, review-first mutation boundary, stale-draft replacement, and complete Restock review remain protected.

Retest only the phase boundary: tap Mic, wait while it says `Wait`, begin speaking only when it changes to `Speak`, say `Restock Zinc syrup 12`, and confirm one complete Restock review with the exact heard words. Close without saving. Do not advance until the startup and ready phases are visibly synchronized on the phone.

Live evidence after `f1a85f9` showed that Chrome's generic recognition `onstart` event still fired before reliable audio capture. Speaking at the first visible `Speak` state produced `restock syrup 12`; the shared matcher then dangerously selected Loratadine from the generic form `syrup`. Another recognition produced `Chris talk zinc syrup 12`, which honestly stayed review-first but lost the restock intent. A complete `restock zinc syrup 12` transcript still produced the correct Restock review. The screenshots also confirmed compact `Speak` and the no-speech recovery layout are readable.

The readiness root now waits only for `onaudiostart`, not the earlier `onstart`, before showing `Speak now.` The shared medicine matcher now rejects queries made only of generic forms or units such as syrup, tablet, bottle, pack, or box. A generic-only restock leaves Medicine blank and blocks `Add stock`; a generic-only sale becomes a blocked Medicine Match review. This rule applies across voice, typed chat, sale, restock, stock lookup, Pharmacy Brain, and Source Brain. It never guesses a medicine from packaging or dosage form alone.

Retest only the smallest safe pair after deployment: first say `Restock syrup 12` after `Speak` and confirm no medicine is selected and saving is blocked; close it. Then say `Restock Zinc syrup 12` after `Speak` and confirm the complete correct Restock review; close without saving. The catalog remains 35 and Zinc stock remains unchanged.

Live evidence after `aed9819` closed the generic-only matcher and voice-restock checkpoint. Replit pulled and restarted with backend health OK. Spoken `restock syrup 12` produced a Restock review with the exact heard transcript, blank Medicine and Unit, quantity 12, blank commercial/traceability fields, and a visibly disabled `Add stock`; no arbitrary medicine was selected. Spoken `restock zinc syrup 12` then produced the complete correct Restock review with the exact heard transcript, canonical Zinc, stock to add 12, unit and form syrup, selling price 70, separate blank bonus stock, and the complete optional delivery and traceability sections. No approval or save is evidenced. Generic-form safety and complete voice-restock review are now protected.

The first spoken-sale attempt exposed an accent and number-word boundary. Android/Chrome transcribed intended `Cefixime 1 cash` as `suffix may 1 cash` and later `suffix may one cash`. The first phrase created an unknown-medicine review; the second did not parse `one` as quantity 1 and still exposed a confirmation-shaped card despite incomplete financial fields. No confirmation or sale is evidenced.

The shared catalog matcher now includes a conservative phrase-level phonetic skeleton after exact and normalized checks. It can compare recognition-shaped multi-word output such as `suffix may` with catalog `Cefixime` without installing a user-specific alias. If multiple strengths or medicines remain equally plausible, the existing ambiguity gate still requires owner choice. The local command parser now normalizes common English and Kiswahili number words to editable numeric quantities for both sales and restocking. A Medicine Match sale cannot confirm until medicine, positive quantity, valid payment, and positive selling price are present. All behavior remains deterministic, local, review-first, and zero-token.

Resume Mic Test 2 with the easier saved catalog name `Zinc`. After `Speak` appears, say `Zinc one cash`. Expect the exact heard transcript, canonical Zinc, quantity 1, Cash, and no automatic sale. Close without Confirm and verify Zinc stock and sales remain unchanged. This simpler phrase simultaneously validates accent-friendly identity, spoken-number normalization, review-first safety, and the three-step maximum: speak, review, confirm.

Live evidence after `4c419d0` passed the spoken-sale interpretation and review boundary. Replit pulled and restarted with backend health OK. Android/Chrome returned the exact transcript `zinc one cash`; the shared local parser converted `one` to quantity 1, the catalog matcher resolved canonical Zinc, Cash was selected, and one editable `Check voice result` card appeared with Confirm, Correct, and Cancel. No automatic sale acknowledgement or mutation is visible in the supplied evidence. Do not repeat the pronunciation or parsing test. Close or Cancel this draft, then provide only the smallest remaining proof that no sale was recorded and Zinc stock stayed unchanged.

Follow-up catalog evidence closed the cancellation/no-mutation boundary. Opening saved Zinc after the voice review showed blank current stock, selling price 70, syrup form and unit, and alias `Zinc syrup`. The Medicine Action Card reported `No changes yet` and kept `Approve & save` disabled. Therefore the unconfirmed spoken sale changed neither stock nor the saved catalog record. The `Zinc one cash` Mic Test checkpoint is complete and must not be repeated without regression evidence.

Bad-pronunciation follow-up evidence completed the final Mic Test 2 boundary. Spoken `suffix may one cash` remained visible as the exact transcript, resolved canonical Cefixime, converted `one` to quantity 1, selected Cash, and stopped at review. After cancellation, saved Cefixime still showed stock 25, selling price 120, tablet form/unit, `No changes yet`, and disabled `Approve & save`. No sale or catalog mutation occurred. There is no remaining Mic Test 2 friction.

The accepted next architecture is the Transaction Completion Engine documented in `transaction-completion-engine.md`. Payment is now one adapter-driven part of shared transaction completion. The local foundation covers Fast Record, Request & Verify state, simulator outcomes, non-blocking pending queue state, global transaction IDs, daily sale numbering, idempotency, duplicate callbacks, and linked undo/reversal records. Real providers are explicitly deferred until simulator-backed owner flows pass.

The next live checkpoint is the first TCE-backed Fast Record sale. Type `Cefixime 1 cash` once. Expect an immediate receipt headed `Sale 1`, canonical Cefixime x1, Cash, and stock left 24. This action intentionally records a real local test sale and reduces Cefixime stock from the protected baseline 25 to 24. Do not repeat or undo until the result is analyzed.

Payment architecture clarification is recorded without changing this active checkpoint. Pharmacy customer funds belong to and go directly to that pharmacy's tenant-isolated authorized merchant account; MS2.0 subscription funds use a separate MS2.0-owned account. Direct merchant connection and a future explicitly approved platform/aggregator model share one adapter contract, but the latter remains disabled without provider and regulatory approval. Provider capabilities must drive available UI actions. No production credentials, callbacks or live payment integration were added.

The Safaricom multi-tenant SaaS onboarding question remains explicitly unresolved and was not guessed. It does not block simulator/TCE development, but direct confirmation from Safaricom or an authorized provider is mandatory before final production onboarding architecture is selected.

Live evidence after `2e64e41` passed the first TCE-backed Fast Record checkpoint. Replit pulled and restarted with backend health OK. Typed `Cefimixe 1 cash` exercised the shared spelling resolver and produced canonical Cefixime. The immediate receipt was headed `Sale 1`, recorded Cefixime x1 as Cash, and reported stock left 24. The complete saved Pharmacy Catalog then showed Cefixime selling price 120 and stock 24. Opening its Medicine Action Card showed the same stock and `No changes yet`, with `Approve & save` disabled. The transaction mutated stock exactly once and created no pending catalog edit.

The next controlled TCE checkpoint is daily sequential numbering across a different medicine. Type `Losartan 1 cash` once. Expect `Sale 2`, canonical Losartan x1, Cash, and stock changing from the protected baseline 40 to 39. Do not repeat or undo until analyzed.

Live evidence after `db95061` passed the second TCE-backed Fast Record checkpoint. Typed `Losartan 1 cash` produced the immediate receipt `Sale 2`, recorded canonical Losartan x1 as Cash, and reported stock left 39. The persisted Pharmacy Catalog independently showed Losartan stock 39 with selling price 25. Its Medicine Action Card retained strength 50 mg, tablet form/unit, buying price 15, supplier Dawa Bora Wholesale Ltd, barcode 6161109876546, batch `LOS-50T`, and expiry `2029-06`; it showed `No changes yet` with approval disabled. Daily numbering continued across medicines, stock mutated exactly once from 40 to 39, and no hidden catalog edit was created.

The Fast Record cash baseline is now live-verified. The next distinct TCE stage is implementation and focused verification of the simulator-backed Request & Verify owner flow and non-blocking Payment Queue. Do not connect a real provider or request credentials. The next owner live command will be issued only after that shared foundation is implemented, regression-verified, documented, committed, pushed, and deployed.

The simulator-backed Request & Verify owner flow is now implemented locally. Payment Queue is a first-class home workspace with owner-selectable Fast Record and Request & Verify modes. Non-cash Request & Verify sales route only to the Simulator adapter, enter a visible waiting queue, leave stock unchanged while pending, and allow serving to continue. Explicit simulated paid/failed controls resolve the queue; only paid applies stock, guarded by a persistent `stockApplied` marker. Focused TCE/UI, medicine matcher, and consistency regressions pass with zero AI calls.

Live evidence passed the Request & Verify pending-safety boundary. The test occurred on the next pharmacy business day, so daily numbering correctly reset to `Sale 1` rather than the incorrectly predicted `Sale 3`. `Losartan 1 mpesa` entered one Simulator-only waiting item, explicitly allowed serving to continue, and left persisted Losartan stock unchanged at 39 with no medicine edit. Three presentation/test-sequence frictions were found: the expected number ignored the business-day reset, generic first-action CSS made Fast Record appear filled even when Request & Verify was selected, and the queue could reopen below its heading. Queue history now labels each item by pharmacy day, only the selected mode is filled, and the queue explicitly opens at the top.

The next live checkpoint after deployment is resolution of the existing waiting transaction. Do not create another sale. Open Payment Queue and first verify the item reads `Today · Sale 1`, Losartan x1, M-Pesa, Waiting, with Request & Verify as the only filled mode. Then tap `Simulate paid` once. Expect the item to leave Waiting, a completed recent entry to remain, a Sale 1 receipt to appear in MS2.0, and Losartan stock to change exactly once from 39 to 38. Do not tap the result twice or create another transaction until analyzed.

Live evidence passed the delayed-resolution state transition. The pending transaction survived into the following day, retained its pharmacy business-day label `2026-07-17 · Sale 1`, showed Losartan x1, M-Pesa and Waiting, and kept Request & Verify as the only selected mode. One tap on `Simulate paid` removed it from Waiting and left one completed history entry. The date label is correct because sale display numbers reset by pharmacy day. A financial-review friction remained: neither the waiting card nor completed history displayed the expected amount, and completed history omitted medicine and quantity. The shared Payment Queue renderer now shows an explicit `Expected amount: KES …` before resolution and preserves date, sale label, medicine, quantity, amount, method and status in history. Receipt and saved-stock evidence are still required before accepting the stock mutation boundary.

Live evidence after `cc718ec` completed the Request & Verify paid checkpoint. Payment Queue showed zero waiting and the complete history row `2026-07-17 · Sale 1 · Losartan x1 · KES 25 · M-Pesa · completed`. Returning to MS2.0 revealed the persisted completion receipt `Sale 1`, `Losartan x1 recorded · M-Pesa`, `Stock left: 38`. The saved Losartan Medicine Action Card independently showed current stock 38, selling price 25, buying price 15 and `No changes yet`, with approval disabled. The simulator confirmation therefore applied stock exactly once from 39 to 38 and created no catalog draft. The owner's initial statement that no receipt was visible reflected its lower position in the existing conversation; the receipt was present and no rendering fix is required.

Live evidence passed the failed-payment boundary. The request showed Today, Sale 1, Losartan x1, M-Pesa and expected KES 25; one simulated failure cleared the waiting queue, retained a failed history row, created no paid receipt, and preserved saved Losartan stock 38 with no draft changes.

Provider and simulator outcomes now use one identity-validating TCE event root; concurrent events are isolated and safe out of order; terminal truth and duplicate callbacks are protected; production hides and rejects simulator controls; failures create durable Notifications; and the redundant home `SHOW ME` tile is removed without changing header, typed/voice, or result-card catalog routes. Automated focused and protected regressions pass.

Live evidence on 2026-07-18 passed the home/catalog protection checkpoint after a clean Replit pull and restart. Home retained only MS2.0 Assistant, Notifications and Payment Queue; the redundant `SHOW ME` tile was absent. The compact header catalog icon opened the complete saved catalog with `Showing 35 of 35`. Closing returned to the unchanged home screen with Notifications at 0 and Payment Queue at 0 waiting; no message, draft, approval, sale or catalog mutation was created. Protect this navigation checkpoint and do not repeat it without regression evidence.

Live evidence on 2026-07-18 passed the complete quiet-concurrency sequence. With Request & Verify selected, Today’s Sale 2 Losartan x1 at KES 25 and Sale 3 Cefixime x1 at KES 120 coexisted as two isolated Simulator-only waiting requests while saved stock remained Losartan 38 and Cefixime 24. Resolving the second request first completed only Sale 3, changed Cefixime exactly once to 23, left Losartan at 38 and retained one waiting request. Resolving Sale 2 then changed Losartan exactly once to 37 and cleared the queue. History retained both truthful completed rows; MS2.0 retained both receipts, including the intermediate one-payment-still-waiting message; saved Medicine Action Cards independently showed Cefixime 23 and Losartan 37 with `No changes yet`; Notifications remained at 0 unread with no routine-success noise.

The automatic-completion simulator live sequence is complete and protected. Do not repeat the cash, paid, failed, home/catalog or quiet-concurrency cases without new regression evidence. No real provider or credentials are connected. Select and prepare the next distinct feature-map stage before issuing another owner live mutation.

The next distinct feature-map stage is Stock fix. Its shared policy requires one canonical saved-catalog medicine, an entered current stock equal to the trusted saved value, a different non-negative whole-number corrected stock, and an audit reason. Approval records canonical identity, adjustment and `queued_not_applied` metadata in the safe offline action queue; it does not directly mutate Pharmacy Catalog stock.

The first manual live attempt failed before queueing. Losartan, current 37, corrected 36 and reason `Physical count test` were visibly retained across the three slides, but Confirm remained disabled and stale guidance still claimed the medicine was missing. Root cause was shared editable-card control drift: the authoritative draft updated, while readiness, button state and owner guidance remained frozen from the initial blank render. The shared lifecycle now synchronizes those controls on every field edit without rerendering or losing slide state, persists one active slide, removes touch-hover false selection, adds concise local Read/Pause/Resume/Stop, and converges manual, Catalog, local picture and guided voice entry into one Stock Fix draft/policy/queue boundary. The checkpoint remains open. Retest manually with Cefixime at protected stock 23, requesting 22 for reason `Physical count test`; do not use Losartan again for this repair pass.

The second manual evidence retained Cefixime 23→22 and the reason across all three correctly selected slides, but guidance falsely said Cefixime had no trusted saved stock and Confirm stayed disabled. Canonical transaction-updated records use `stockLeft`; the Stock Fix reader omitted that shape. The shared trusted-stock boundary now accepts canonical `stockLeft` alongside protected legacy shapes. The same evidence showed that Android/Chrome Pause/Resume was unreliable for one long utterance and Read did not move the visible slides. Read now uses three deterministic local segments tied to slide movement; Pause retains the current segment, Resume restarts it and continues, and Stop ends the review. The checkpoint remains open at the interrupted Cefixime manual confirmation; no correction was queued and saved Cefixime remains 23.

Latest evidence passes the repaired Stock Fix Read flow, automatic section movement, pause/resume and ready Confirm state. These are protected and must not be repeated. No queued-result evidence was supplied, so the manual checkpoint remains open only at Confirm. The resumed test is at most three steps: pull/restart and refresh, tap Confirm once on Cefixime 23 to 22, then verify `Stock fix waiting to sync` while the saved catalog stock remains 23.

Corrected product intent supersedes that queue-first acceptance criterion. The supplied Cefixime 23 to 22 result displayed `Pending sync` and independently retained saved stock 23; this is product friction, not a successful online Stock Fix.

The shared confirmation architecture now applies a validated correction to durable local catalog state immediately when online, records one audit completion, and creates no pending duplicate. Offline or unavailable durable storage retains one idempotent correction; app startup and reconnect retry it automatically. The next live checkpoint uses a different medicine: note Losartan's current saved stock, enter a different corrected quantity and reason, Confirm once, then verify `Stock updated` and the new quantity immediately in Pharmacy Catalog. Do not repeat Read or begin picture assistance.

Live evidence after `9deb92f` passes the corrected manual online Stock Fix checkpoint. On restart, the former pending Cefixime correction applied automatically once and reported `Stock updated. Cefixime: 23 → 22.` Losartan then started from independently saved stock 37; one Confirm reported `Stock updated. Losartan: 37 → 39.` Reopening the saved Losartan record showed Current stock 39, retained commercial fields, `No changes yet`, and disabled `Approve & save`. No second approval or manual Sync was required.

Manual Stock Fix is complete and protected; do not repeat Cefixime, Losartan, Read, or immediate-apply behavior without regression evidence. The next distinct Stock Fix stage is picture-assisted entry with another catalog medicine. Do not issue that owner test until a realistic non-overlapping controlled package and exact trusted saved-stock baseline are prepared and focused-protected.

Picture-assisted Stock Fix is now prepared with the repository fixture `ms20-main-app/fixtures/stock-fix-prednisolone-5mg.png`. It is a realistic synthetic package showing only Prednisolone, 5 mg, tablets, 100 tablets, batch `PRE-5T` and expiry `2028-11`; it does not claim stock, price, supplier or pharmacy ownership. One isolated Stock Fix registry matches filename, exact content hash or bounded local visual identity, then reads trusted current stock 24 only from the saved Pharmacy Catalog. It cannot leak into ordinary medicine-photo onboarding, unknown images remain unmatched, and all processing is local and zero-token.

Next live checkpoint: pull/restart, download the fixture from `/main-app/fixtures/stock-fix-prednisolone-5mg.png`, then open Stock Fix and choose More → Photo. The picture must fill canonical Prednisolone and current stock 24 without filling corrected stock or reason. Enter corrected stock 23 and reason `Picture count test`, Confirm once, and verify `Stock updated. Prednisolone: 24 → 23.` plus saved catalog stock 23. Do not test Read, camera, voice, or another medicine.

First picture-checkpoint evidence exposed one shared presentation defect before photo selection: the native More panel opened inside a horizontally clipped medicine carousel, so its absolute menu was invisible and More appeared unresponsive. The panel now expands in normal card flow across all three action columns and remains inside the slide's vertical scroll area. No Stock Fix was confirmed and Prednisolone remains at the protected baseline 24; resume only at More → Photo after deployment.

Follow-up evidence passes the repaired More panel and native Photo picker, but the selected Google Photos representation returned to an unchanged blank draft. The mobile picker created a cropped/re-encoded visual composition outside the initial generated-file references, and Stock Fix then hid its photo-specific failure behind generic medicine readiness. The isolated registry now includes only the bounded visual references verified from this selected package composition, retains strict aspect/hash safety, and the card shows the honest photo result whenever identity remains blank. Prednisolone is still 24; resume at Photo selection only.

The next evidence proves the honest failure message but also proves Google Photos still does not return stable controlled-fixture bytes or a matching bounded visual identity. Do not weaken recognition further. Stock Fix More now adds a File choice routed through the same photo processing boundary so the owner can select the committed PNG from Downloads with its exact filename/content hash; ordinary Photo and Camera remain available for real acquisition. Prednisolone remains 24. Resume only with More → File and the downloaded PNG.

Android still routed that shared image-capable File input through Google Photos. The controlled fixture now also ships as `stock-fix-prednisolone-5mg.ms20image`, byte-for-byte identical to the verified PNG, and Stock Fix File uses a dedicated octet-stream input that cannot advertise itself as a photo picker. The SHA-256 gate remains unchanged. Download the `.ms20image` file and resume only with More → File; Prednisolone remains 24.

That fixture-transport workaround is superseded. The shared root was inconsistent acquisition plus exact-file identity: Android could re-encode Photo evidence, while the special File path increased memory pressure and still bypassed real recognition. Camera, Photo and File now use normal image inputs, bounded local preparation, one cancellable local OCR route, one catalog-first normalized result, and one Stock Fix draft; Read stops before media work and failed attempts can retry without refresh. Guided microphone entry now continues medicine → stock → reason → review → spoken Confirm on the same draft. Prednisolone remains 24; the next checkpoint is Test 1 Photo only.

Live evidence on 2026-07-18 passes Stock Fix Test 1 — Photo. Replit pulled the shared media repair and restarted with backend health OK and WhatsApp disabled. Android Photo selection populated canonical Prednisolone and trusted saved stock 24 without a low-memory failure; the owner entered corrected stock 23 and reason `Picture count test`, then confirmed once. MS2.0 reported `Stock updated. Prednisolone: 24 → 23.` The independently opened saved Medicine Action Card showed Prednisolone 5 mg, tablet form/unit, Current stock 23, retained prices, `No changes yet`, and disabled approval, proving immediate durable application with no catalog edit or pending duplicate.

Photo is complete and protected. Do not repeat Prednisolone, manual Stock Fix, or Read without regression evidence. The next distinct checkpoint is Stock Fix Test 2 — Camera with a different saved medicine and trusted stock baseline; prepare that single test before issuing owner actions. Do not begin File or microphone in the same session.

Stock Fix Test 2 — Camera is prepared with `ms20-main-app/fixtures/stock-fix-metronidazole-400mg-camera.png`. The controlled carton visibly supports only Metronidazole, 400 mg, tablets, 20 tablets, batch `MET-400C` and expiry `2029-03`; it contains no stock, price, supplier or pharmacy-ownership claim. Trusted current stock 36 comes only from the already saved Pharmacy Catalog baseline. The required owner correction is 35 with reason `Camera count test`. Focused protection binds the committed bitmap hash, Camera normalized evidence contract, canonical catalog match, catalog-authoritative stock, supported package fields, and blank corrected stock/reason. The next live session is Camera only; File and microphone must not begin.

Initial Camera evidence on 2026-07-18 was inconsistent across two clear captures of the same controlled carton. The first completed local processing but returned no safe catalog match; the second populated Metronidazole 400 mg and trusted saved stock 36, then one confirmation applied 36 to 35. The package and framing were readable, so this is shared local-OCR evidence-selection friction rather than an owner capture failure. The backend selected only the longest of two OCR passes, which could discard a shorter pass containing the medicine name. It now preserves the unique union of complementary lines across normal and binary local layout passes, so any pass that reads the canonical name remains available to the catalog matcher.

The same evidence exposed rush-hour friction: reason was mandatory even though medicine, authoritative current stock and corrected stock already define a safe correction. Reason is now optional across shared validation, UI labeling/guidance, voice prompts, spoken review and audit persistence. A supplied reason remains preserved; an omitted reason is recorded as blank and reviewed as `not provided`, never invented. The repair checkpoint remains Camera-only: repeat the same Metronidazole capture twice after deployment and require the same canonical match/current stock 35 both times, then confirm one correction to 34 with Reason blank. File and microphone remain blocked.

Post-repair evidence proves optional Reason passed end to end: the UI labeled it optional, blank Reason allowed ready Confirm, one confirmation applied Metronidazole 35 to 34, and the independently opened saved Medicine Action Card showed Current stock 34 with retained strength, form, unit and prices and no catalog edit. Camera consistency did not pass: the first new capture still produced no match and the next capture populated canonical Metronidazole/current stock 35. The remaining shared root is whole-frame dependence plus name-only identity: the phone camera includes surrounding laptop text, and a clear package may expose batch/strength/expiry even when OCR misses the printed medicine name.

Local OCR now adds bounded package-region layout passes while retaining whole-frame evidence. The normalized catalog matcher may recover identity only from a unique exact saved barcode, a unique exact saved batch, or a unique strength-plus-expiry pair; strength alone and duplicated identifiers never select a medicine. Trusted stock still comes only from that matched saved catalog record, and corrected stock remains owner-only. Repeat Camera acquisition twice from the saved Metronidazole baseline 34; both reads must resolve the same medicine/current stock before any further mutation. File and microphone remain blocked.

The next two-capture evidence after `b9aea88` again produced one no-match followed by one correct Metronidazole/current stock 34 result. No stock mutation occurred. This proves the first bounded crop was still too broad and too small when the phone photographed a carton displayed within surrounding laptop content; on the failed frame, OCR did not recover even the strong package identifiers needed for safe catalog recovery. The shared OCR boundary now uses a tighter lower-center package region, enlarges it to a stable OCR width, evaluates normal and binary layouts, and separately reads the upper-package area where medicine name and strength usually appear. Whole-frame evidence and all strict identity/stock safeguards remain unchanged. Repeat only the two no-mutation Camera reads; File and microphone remain blocked.

Live evidence after `74f7f69` passes the repaired Camera consistency checkpoint. The repeated Camera-only sequence produced the same populated Stock Fix draft: canonical Metronidazole, recognized 400 mg package strength and trusted saved Current stock 34. Medicine, Stock and Reason sections retained the same values; Correct stock and optional Reason remained blank, Confirm stayed safely blocked for missing correction, and no stock mutation or pending action occurred. Camera is complete and protected. Do not repeat Metronidazole Camera, its optional-reason correction, or prior Photo/manual tests without regression evidence. The next distinct Stock Fix source is File with another supported saved medicine image; prepare it separately before issuing owner actions. Microphone remains later.

Stock Fix Test 3 — File is prepared with `ms20-main-app/fixtures/stock-fix-ibuprofen-200mg-file.png`. The generic carton visibly supports only Ibuprofen, 200 mg, tablets, 24 tablets, batch `IBU-200C` and expiry `2028-12`; it contains no stock, price, supplier, barcode or pharmacy-ownership claim. Trusted Current stock 28 comes only from the already saved Pharmacy Catalog record. The owner correction is 27 with optional Reason left blank. Focused protection binds the committed file hash, normal image File route, shared OCR/evidence contract, canonical and strong-identifier matching, catalog-authoritative stock, retained package fields, and blank correction/reason before owner entry. Test File only; do not begin microphone.

Live evidence on 2026-07-18 passes Stock Fix Test 3 — File. Android selected the normal downloaded PNG through its image provider; the evidence remained inside Stock Fix and populated canonical Ibuprofen, recognized strength 200 mg and trusted saved Current stock 28. Correct stock 27 with blank optional Reason reached ready Confirm. One confirmation reported `Stock updated. Ibuprofen: 28 → 27.` The independently opened saved Medicine Action Card showed Ibuprofen 200 mg, tablet form/unit, Current stock 27, retained selling price 18 and buying price 9, `No changes yet`, and disabled approval, proving immediate durable application with no onboarding leak, catalog edit or pending duplicate. File is complete and protected. Do not repeat manual, Photo, Camera or File without regression evidence. The next distinct Stock Fix source is microphone with a different harder-to-pronounce saved medicine; prepare it separately before testing.

Stock Fix Test 4 — guided microphone is prepared with saved Co-Amoxiclav at the independently verified catalog baseline 24. It is a distinct harder-to-pronounce medicine and requires no image source. The owner will speak the medicine, then corrected stock 23, leave Reason blank, and say Confirm. Because Reason is optional, the first spoken Confirm must now start the complete three-section local review rather than mutate immediately; after the review the microphone resumes, and only a second spoken Confirm may apply 24 to 23 once. Any uncertain medicine result must ask for an exact saved name and must not fill stock. Live acceptance requires canonical Co-Amoxiclav, catalog-authoritative current stock 24, visible blank optional Reason, spoken review including `not provided`, one immediate `Stock updated. Co-Amoxiclav: 24 → 23.` result, independently saved stock 23, and no pending duplicate or learned alias replacing the canonical name. Test microphone only.

The first live microphone attempt failed before interpretation: Android remained visibly in `Speak`, more than ten medicine-name repetitions produced no displayed transcript, and the blank Stock Fix card retained its initial missing-medicine guidance. This is capture observability and termination friction, not a pronunciation-match result. The shared browser speech boundary now renders interim and final `Heard: “…”` text, stops after detected speech or an eight-second bound, reports an explicit no-completed-transcript result, and pauses after an unmatched or ambiguous medicine instead of silently reopening the microphone. No stock changed; Co-Amoxiclav remains 24. Repeat only the medicine-name capture after deployment and return the exact visible transcript before advancing to stock.

Post-observability evidence exposed the remaining guided-conversation friction without mutating stock. Android visibly transcribed variants including `call amoxic love` and `call amoxiclav`; a later safe match filled canonical Co-Amoxiclav and trusted stock 24. Stage state then drifted: the card could retain the Medicine section or prompt `Say the medicine name` after a successful match, `current stock 24` and `new stock 23` required separate attempts because the parser did not understand ordinary `new stock` phrasing, and the final ready card could reopen with the wrong prompt or end with a no-transcript message instead of an acknowledgement. Correct stock 23 was drafted but never confirmed; saved Co-Amoxiclav remains 24.

Guided Stock Fix now derives its stage from completed authoritative fields rather than carousel timing. Every final utterance is retained in a visible card-level Voice transcript. Medicine-prefix phrases are normalized before catalog-first matching; safe non-canonical spoken matches are cached only in pharmacy-scoped pronunciation memory while the canonical name remains unchanged. The Stock stage accepts `current stock is 24, new stock is 23`, `new stock 23`, the existing correct/set forms, and a current-stock acknowledgement. The Reason stage accepts a supplied reason or explicit `no reason`. After each accepted stage, device speech synthesis acknowledges the values, moves the card, states exactly what to say next, and only then reopens device recognition. The first Confirm still triggers the complete local review and only the post-review Confirm can mutate once. Deploy this repair, then finish Co-Amoxiclav 24 to 23 before preparing a different-medicine consistency test.

Live evidence after `1b4de86` proved the guided Co-Amoxiclav correction applied exactly once from trusted stock 24 to 23. The bounded Voice transcript retained the medicine variants, stock values, explicit `no reason`, and final Confirm; the success result named canonical Co-Amoxiclav 24 to 23, and the independently reopened Medicine Action Card showed saved stock 23, `No changes yet`, disabled approval, and no visible duplicate. The same card contradicted that truthful state with a hard-coded `Unsaved draft` badge. Medicine Action Card presentation is now derived from the shared catalog review result: unchanged records say `Saved medicine`, real edits say `Unsaved changes`, invalid edits say `Needs attention`, and the status and description update with field edits. Focused catalog protection covers all three states. After deployment, reopen saved Co-Amoxiclav and verify the truthful saved state; then capture the complete guided review on one separate different-medicine consistency test before its post-review Confirm.

Live evidence after `a7fbc82` closes the Medicine Action Card consistency repair. Replit restarted with backend health OK and WhatsApp disabled. The independently reopened canonical Co-Amoxiclav record showed `Saved medicine`, Current stock 23, the complete retained catalog values, `No changes yet`, and disabled approval. No field was edited and no pending draft or duplicate was visible. The separate guided-voice consistency checkpoint is Cetirizine, whose last independently verified saved baseline is 45. Use one blank Stock Fix, speak `Cetirizine`, then `current stock is 45, new stock is 44`, then `no reason`. Stop and capture the complete Medicine → Stock → Reason review before saying the post-review Confirm. Do not mutate Cetirizine until that review evidence is accepted.

Cetirizine evidence exposed a safety-state collision. Medicine 45, new stock 44 and explicit blank Reason were retained correctly, but the first spoken Confirm immediately applied 45 to 44 without presenting the promised review. The independently reopened saved card confirms one durable update to 44, canonical identity, `No changes yet`, and disabled approval. Root cause: the policy's `result.review` meant the draft was ready for review, but the UI treated it as `reviewedSlides`, so completing Reason pre-satisfied the later Confirm gate. Guided review now has separate started and completed states. Reason readiness cannot set either state or start review; first Confirm starts the three-section review; execution remains blocked during or after an interrupted review; only speech-synthesis completion marks review complete; subsequent field changes invalidate it; and only the next Confirm can execute. Retest Cetirizine from the now-authoritative 44 to 43 after deployment and stop after the completed review before the post-review Confirm.

Live evidence after `8e273f6` proves the first Confirm no longer mutates: Cetirizine remained at saved stock 44 while the transcript retained Cetirizine, new stock 43, blank Reason and one Confirm, and the card reported that the complete review finished. Owner evidence also exposed avoidable two-confirm friction: the second-confirm requirement was not explained early enough, and an automatic silent listening timeout replaced the useful instruction with a generic no-transcript message. The shared guided confirmation boundary now covers both spoken and tapped Confirm, explains before review that Confirm is required once to review and again to apply, marks the first Confirm visibly, ends the spoken review with the exact second action, and preserves that action if resumed listening hears silence. Retest from one fresh Cetirizine 44 to 43 draft, verify the guidance and review, then say the clearly requested second Confirm once.

Live evidence after `a2211a0` closes Guided-Voice Stock Fix. Replit restarted with backend health OK and WhatsApp disabled. A fresh Cetirizine draft retained canonical identity, authoritative current stock 44, corrected stock 43, blank Reason and transcript history. The first Confirm visibly stated that Medicine, Stock and Reason were being reviewed and explicitly requested Confirm again afterward; the second Confirm produced exactly one `Stock updated. Cetirizine: 44 → 43.` Saved Cetirizine independently showed stock 43, complete retained fields, `No changes yet`, disabled approval and no visible duplicate. Manual, Photo, Camera, File and guided microphone Stock Fix are complete and protected. The next feature-map stage is Report. Begin read-only: open `+` → `Report`, inspect the single Today report request card and return it without confirming; no sale, stock, catalog, approval, queue or download may change.

The first Report evidence exposed a technical placeholder rather than an owner workflow: the card exposed an internal backend URL, generic Confirm only saved a request, and `Download CSV` invoked the Pharmacy Catalog export rather than a report file. The Report action now shows only owner fields, uses `Generate report`, posts to the existing daily-report service without WhatsApp delivery, renders the returned multiline report read-only, supports truthful refresh and offline/backend errors, and never labels catalog CSV as a report download. Focused report and architecture verification pass. After deployment, generate Today’s report once and return the complete visible report; do not test document downloads yet.

Post-deployment evidence showed the daily-report request succeeded (`Today's report` and `Refresh report`) but its content remained invisible. Root cause was the shared editable-card schema: Report still declared the removed `backend_route` field and omitted `report_date` and `report_text`; the owner note also hard-coded the obsolete request-saving instruction. The canonical Report schema now renders period, focus, returned date and read-only multiline report, excludes the technical route, and displays the actual generation success or failure guidance. Refresh the existing generated report card after deployment and require visible report content before advancing.

Authoritative report evidence on 2026-07-19 showed zero sales, which is honest for the tested business day (stock corrections only), but exposed shared refresh and presentation defects. Preview refresh incorrectly appended another daily-report history record, identical snapshots had no generated-at evidence, the report duplicated Sales/Total sales, and the global developer `<pre>` styling clipped owner content. Report preview/refresh now bypasses caches, recomputes read-only from the saved sale ledger/activity/current-stock sources, returns and displays Nairobi freshness, writes no duplicate report row when WhatsApp sending is disabled, and uses an unclipped owner report surface. Export-document production-quality testing remains at the scheduled Export Hub checkpoint.

Post-deployment evidence after `9fbf0d2` showed the refreshed request never settled: the legacy snapshot remained, Generated At was blank, and the validation stayed in its generating state. The shared recovery boundary now treats `submitting` as ephemeral rather than durable, marks legacy report freshness honestly, restores interrupted cards to a retryable state, and aborts an owner refresh after 20 seconds without destroying the prior report. Ledger-backed generation also avoids the redundant transaction-sheet read. This prevents permanent loading traps across every resumed report while retaining truthful errors and read-only semantics.

Live evidence after `db6dbd1` closes daily-report generation and refresh: freshness advanced, duplicate totals disappeared, the complete source-qualified snapshot rendered, and no WhatsApp or duplicate report save occurred. Before the next physical Read check, the generic card narrator was found to concatenate raw Report fields and repeat date/freshness around the full body. Report Read now uses the canonical report body alone, normalizes currency/payment pronunciation, and refuses truthfully when no report exists. The active checkpoint is one clean on-device Read with no mutation; document exports remain deferred to Export Hub.

Report Read content passed after `1124e37`, but Android Chrome delayed all editable-card narration by about five silent seconds. The shared speech path was cancelling even an idle synthesis queue immediately before speaking, and did not warm the available voices. A single speech boundary now warms voices, speaks directly when idle, and only cancels/restarts briefly when replacing active speech. This root fix covers Report, generic editable cards, and guided Stock Fix narration. The active checkpoint is one on-device report Read start-latency retest; document exports remain deferred.

Speech start passed after `f691df2` at about one second. Reporting now permanently includes deterministic historical periods: Today, Yesterday, ISO date, Last 7 days, This/Last week, This/Last month and inclusive ISO custom range. Period resolution rejects future/reversed/ambiguous ranges; generation reads the exact saved transaction/log interval, prefers transaction finance rows for cost/profit accuracy with ledger fallback, suppresses present-day low-stock state on past-only reports, remains read-only/no-cache, and keeps generation freshness outside the deterministic report body. Expense, credit, restock and inventory-history coverage must expand only alongside their canonical ledgers; missing sources are never guessed. Generic editable-card narration now has a shared visible Pause/Resume/Stop lifecycle. The active live checkpoint is Yesterday plus those speech controls; Export Hub remains later.

The first historical Yesterday test returned the correct requested date and saved-record zero totals, but took about one minute; Pause worked, Resume did not, and Stop worked. Yesterday was incorrectly routed through daily generation, triggering a current-stock lookup, while logs and transactions were fetched in separate full-sheet calls. Historical reports now use one filtered Google Sheets batch request for both canonical sources and never query/present current stock as historical. Historical titles, source text and guidance are explicit. Resume now follows MS2.0's own paused state rather than Android's inconsistent synthesis flag. The active checkpoint is a timed `2026-07-18` refresh (target under 10 seconds) plus one Pause/Resume/Stop cycle.

The next delta was captured during Wi-Fi loss: Yesterday timed out at the 20-second boundary and correctly retained the old July 19 snapshot. Wi-Fi is now restored. Period selection is no longer a typing field: one native picker covers all permanent presets, with native calendar inputs for custom day/range, all normalized by the same backend parser. The shared editable-card reader now uses safe line/sentence segments, retains its segment index across Pause, resumes from the nearest safe boundary, and clears on Stop, refresh, period change or card close. Report generation disables duplicate taps and cancels stale requests. The active checkpoint is a single Last 7 days picker/refresh timing plus Read/Pause/Resume; Export Hub remains later.

Owner evidence on 2026-07-19 confirms the period picker and segmented Read/Pause/Resume controls work, but both Last 7 days and Yesterday refresh attempts ended in the safety-timeout message. The retained `2026-07-19` Report Date and `19/07/2026, 03:45:26` Generated At therefore correctly described the last successful report; they were not fresh results for the newly selected period. Report selection now immediately changes the title and marks retained content as the last successful report, labels its metadata `Displayed Report Date` and `Last Generated At`, and explicitly requires Refresh for the selected period. Successful generation reports measured elapsed seconds and is the only path that replaces content/freshness. The client safety boundary now matches the accepted maximum of 10 seconds; timeout guidance names that bound and explains why retained metadata is intentionally unchanged. The next live test remains one Last 7 days refresh after deployment, with the Replit backend log required if it exceeds 10 seconds. Do not test exports.

Post-deployment evidence proves the stale-report presentation repair passed, but the request again aborted at 10 seconds. Independent uncached calls to the same deployed endpoint completed correctly in 16.7 seconds, followed by 18.9, 15.6 and 17.1 seconds; Replit health alone took 4.7 seconds. The returned report had the correct inclusive `2026-07-13 to 2026-07-19` range, historical saved-record source and no current-stock-as-history claim. The fixed 10-second client cutoff, not an invalid backend result, was therefore the immediate failure cause on this live Replit/Google Sheets path. Historical reads remain uncached and source-truthful. The client now shows explicit still-working guidance at 10 seconds and retains a bounded 30-second safety cutoff, covering the measured distribution without duplicate requests or stale replacement. Successful completion still exposes measured elapsed seconds for continued performance evidence. Next live test is one Last 7 days refresh only; do not repeat speech or test exports.

Functional evidence after `8b638a7` passed, but reporting performance remains open. The single Last 7 days refresh completed in 19.1 seconds without a duplicate tap and correctly replaced the retained Today snapshot with inclusive Displayed Report Date/body range `2026-07-13 to 2026-07-19`, advanced Last Generated At to `19/07/2026, 11:22:15`, rendered saved-record zero totals, omitted current stock from historical output, and confirmed no WhatsApp send or duplicate save. This was not a one-time startup delay: independent uncached production requests measured 16.7, 18.9, 15.6 and 17.1 seconds. The picker, stale-report presentation, historical truthfulness and Read/Pause/Resume are accepted and protected, but the 1–3 second ordinary-generation target is not complete. Keep Export Hub blocked. The active engineering checkpoint is root-cause profiling and repair of persistent report latency without caching, fabricating data, duplicating source reads or weakening freshness.

Evidence after documentation-only checkpoint `3bf0b4c` measured another Last 7 days refresh at 27.1 seconds. The range, freshness and source truthfulness still passed, but latency worsened and remains a blocking performance defect. This deployment contained no performance code and therefore cannot close or validate the performance target. Do not issue another owner refresh until an actual backend source-read repair is implemented and independently timed. The synchronous uncached Google Sheets combined read remains the leading bottleneck; timeout extensions are not a performance repair.

Root performance repair is now implemented in the shared Google Sheets repository. The former path made the owner request wait for the one combined Daily Log/Transactions network read every time; production evidence placed that dependency at 15.6–27.1 seconds while report filtering/construction remained local. After schema setup, each pharmacy-scoped store now warms one bounded 20,000-row canonical source snapshot, refreshes it every five minutes, retains it for at most ten minutes, coalesces concurrent cold reads, and updates it immediately after every successful in-app Daily Log or Transaction write. Cache state is instance-scoped, so pharmacies cannot share rows. Overflow or warmup failure falls back to the authoritative Sheets read. Today, Yesterday, all historical/custom periods, daily-log readers and transaction readers inherit the same repair; stock/catalog paths remain unchanged. A deterministic delayed-source profile used one 250.6 ms cold fetch, then measured repeat 0.057 ms, different period 0.037 ms, post-write 0.038 ms and daily-log read 0.042 ms with one API call. Production performance remains open until deployment evidence records first and immediate refreshed Last 7 days timings. Export Hub stays blocked.

Deployment evidence after `b8d738e` failed performance again: the first Last 7 days refresh took 19.8 seconds and the immediate second took 26.0 seconds. The Replit shell did not show the required warmup-complete line, proving the snapshot was not demonstrably available. The leading concrete retention fault is the initial 20,000-row ceiling: an oversized combined source is served truthfully but intentionally not retained, which recreates the network wait on every request. The bounded ceiling is now 100,000 rows; warmup prints exact retained log/transaction counts to the Replit shell, and the report response includes non-sensitive readiness, row-count, age, hit and miss diagnostics. Tests protect one miss/one hit, concurrency coalescing and cross-store isolation. Performance remains open pending visible warmup plus two timed cache-hit reports.

Evidence after `4081134` proves the source snapshot was ready and retained (`REPORT_SOURCE_SNAPSHOT_WARMED logs=0 transactions=0`), yet two reports still took 14.5 and 20.5 seconds. This ruled out Sheets, filtering and cache retention. The remaining synchronous stage was `ReportService._recommendations`, which called OpenAI on every routine report even though deterministic recommendations already existed. That network/token call was the actual post-cache bottleneck and violated MS2.0's deterministic AI-last rule. ReportService now always uses local deterministic recommendations and the production factory injects no recommender. A regression test supplies a recommender that raises if invoked. Independent complete report construction measured 112.3 ms cold, 0.212 ms repeat and 0.173 ms for a different period with zero AI calls. All report periods inherit the repair. Production performance remains open for one final two-refresh deployment timing; Export Hub remains blocked.

Permanent enforcement now closes the broader architectural gap: no routine operational workflow may call an LLM or paid AI API without an approved entry in `app/ai_policy.py`. The registry is machine-readable and fail-closed, contains every required value/cost/fallback/timeout/retry/cache/privacy/ownership field, and currently approves only voice-note transcription, ambiguous command parsing after deterministic failure, and explicitly enabled review-first photo/invoice extraction. OpenAI client construction is confined to the two guarded wrappers, automatic retries are disabled, and timeouts are bounded. Report AI recommendations remain prohibited even through legacy compatibility injection. Static and behavioral regression tests protect this boundary.

Warmup observability is also repaired without changing warmup data behavior. Uvicorn output is redirected to `server.log`, which is why the background marker existed but was absent from the Replit shell. The manual `grep -a "REPORT_SOURCE_SNAPSHOT_WARMED" server.log | tail -1` worked because `-a` treats binary-detected log content as text, the phrase selects only the event, and `tail -1` selects the newest occurrence; it only reveals a marker and does not trigger warmup. Startup now performs the equivalent bounded recent-tail check after health, prints the real marker once, and warns after 15 seconds if none exists. The active checkpoint remains two immediate Last 7 days refreshes after automatic warmup output; capture both generation times and Generated At values. Performance and Export Hub remain open until this live evidence passes.

Live deployment evidence after `342c359` closes report performance and the zero-AI production checkpoint. The Replit shell automatically printed the truthful marker `REPORT_SOURCE_SNAPSHOT_WARMED logs=0 transactions=0`. Two immediate Last 7 days refreshes completed in 0.6 seconds and 0.3 seconds, with `Last Generated At` advancing from `19/07/2026, 20:00:32` to `19/07/2026, 20:00:44`. Both rendered the inclusive `2026-07-13 to 2026-07-19` range, saved sales/activity source, no present-stock-as-history claim, and explicit no-WhatsApp/no-duplicate-save guidance. OpenAI Platform remained unchanged before and after at 6,476 tokens, 22 requests, USD 0.31 spend and USD 3.40 credit. Two additional Yesterday refreshes completed in 0.3 seconds with correct `2026-07-18` historical output, providing regression evidence without reopening that accepted period. Reporting is complete and protected.

The next ordered stage is Export Hub integrity. Repository inspection confirms that only the earlier catalog CSV download exists; no owner-facing shared Export Hub or genuine Excel, PDF and Word generators are yet implemented. Do not ask the owner to test nonexistent controls. The active engineering checkpoint is the smallest shared, deterministic, canonical export boundary: inventory/catalog owner downloads first, with accurate pharmacy identity and Kenya time, purpose-specific readable output, safe filenames, phone/desktop download behavior, and zero AI/API formatting. Implement and verify locally before issuing one live action.

The shared Export Hub is now implemented from one pharmacy-scoped canonical inventory document model. The `+` menu exposes one mobile-first Export Hub with CSV, Excel, PDF, Word, Presentation and Print choices. CSV remains UTF-8 machine-readable interchange; Excel is a genuine formatted `.xlsx` workbook with typed numbers, frozen header, filter, widths and landscape print setup; Word is a genuine landscape `.docx` inventory table; PDF is a real paginated landscape document; Presentation is a genuine 16:9 `.pptx` owner briefing split across bounded inventory slides; and Print opens a responsive, repeating-header print view. Every renderer consumes the same immutable canonical rows, pharmacy identity and Africa/Nairobi timestamp, uses truthful safe filenames, runs locally, performs no network or AI call, and cannot read another pharmacy's state.

Focused verification passes JavaScript compilation, shared canonical values, 35-row scaling, pharmacy isolation, CSV escaping/BOM, genuine ZIP/OOXML signatures, PDF structure, mobile Export Hub wiring, safe download names, print layout and static zero-AI protection. Independent readers opened the representative 35-row files as one Excel sheet, one 35-row Word table, a two-page PDF and a four-slide presentation. Catalog workspace, report workflow and architecture regressions also pass. The first live Export Hub checkpoint is CSV only: after deployment, open `+` -> `Export Hub`, verify it says 35 medicines and identifies the current pharmacy/branch, tap CSV once, and return the visible success message plus the downloaded filename and Android Downloads screenshot. Do not open or test Excel, PDF, Word, Presentation or Print yet.

Available live evidence after `32bcaef` confirms the Replit pull/start, backend health, the `+` menu Export Hub entry, a mobile Export Hub showing `35 medicines`, `Your pharmacy · Main`, all six deterministic format choices, and the visible `Generated locally · Pharmacy-isolated · Canonical data · Zero AI formatting` boundary. The screenshot still says `None yet` and shows generic inherited Confirm/Read/Correct/Cancel actions; no CSV success message, downloaded filename or Android Downloads entry is supplied. Therefore CSV acquisition is incomplete, not failed. Do not advance to Excel and do not infer a download. Request only one combined screenshot after tapping CSV once that shows the in-app success state and Android Downloads entry/filename.

Follow-up evidence on 2026-07-25 closes CSV acquisition. One tap produced the browser `File downloaded (2.53 KB)` notification and the in-app truthful state `CSV downloaded for 35 medicines at 25/07/2026, 01:11:59 Africa/Nairobi.` Android Downloads showed one new `your-pharmacy-inventor…` spreadsheet entry of 2.53 KB from one minute earlier. The Export Hub continued to display local, pharmacy-isolated, canonical, zero-AI generation; OpenAI Platform showed zero tokens, zero requests and unchanged USD 3.40 credit. No file was opened, no second format was tested, and no unrelated mutation is evidenced. CSV download acquisition is passed and protected. The next checkpoint is Excel acquisition only; do not open the workbook yet.

The same evidence exposed one shared presentation defect: the read-only Export Hub inherited generic Confirm, Read, Correct and Cancel actions after its own immediate format buttons. Export generation requires no approval or correction workflow, so these controls were misleading and could route an owner into unrelated generic card behavior. `activeActionsTemplate` now treats Export Hub like the Catalog workspace and renders no inherited actions; format buttons, truthful status, technical details and explicit close controls remain. Focused verification protects the boundary. Deploy this repair before the Excel acquisition checkpoint.

Live Excel acquisition after `f137c10` passes. The cleaned Export Hub retained `35 medicines`, `Your pharmacy · Main`, the six format choices and local/pharmacy-isolated/canonical/zero-AI wording while the inherited Confirm/Read/Correct/Cancel buttons were absent. One Excel tap produced `XLSX downloaded for 35 medicines at 25/07/2026, 01:21:48 Africa/Nairobi`, the browser confirmed one 29.49 KB file, and Android Downloads showed exactly one new 29.49 KB `your-pharmacy-inventor…` workbook above the earlier 2.53 KB CSV. OpenAI remained at zero tokens and zero requests. The workbook was not opened and no other format was tested. Excel acquisition is passed and protected.

The evidence also showed the generic note `Check the details, then confirm.` survived after the actions were removed. This contradicted the immediate read-only download behavior. `ownerCardNote` now gives Export Hub the truthful instruction `Choose a format to download. No confirmation is required.` The shared format architecture is unchanged. Deploy this copy repair before the PDF acquisition checkpoint.

Live PDF acquisition after `840642a` passes. The Export Hub displayed the corrected `Choose a format to download. No confirmation is required.` guidance with no inherited approval controls. One PDF tap produced `PDF downloaded for 35 medicines at 25/07/2026, 01:29:14 Africa/Nairobi`, the browser confirmed one 13.63 KB file, and Android Downloads showed exactly one new 13.63 KB PDF above the protected 29.49 KB XLSX and 2.53 KB CSV. The shared local/pharmacy-isolated/canonical/zero-AI boundary remained visible and OpenAI showed zero tokens and requests. The PDF was not opened and Word was not tested. PDF acquisition is passed and protected; Word acquisition is next.

Live Word acquisition passes. One Word tap produced `DOCX downloaded for 35 medicines at 25/07/2026, 01:34:13 Africa/Nairobi`, the browser confirmed one 59.65 KB file, and Android Downloads showed exactly one new 59.65 KB Word document above the protected PDF, XLSX and CSV entries. The truthful no-confirmation guidance and shared local/pharmacy-isolated/canonical/zero-AI boundary remained visible; OpenAI showed zero tokens and requests. The DOCX was not opened and Presentation was not tested. Word acquisition is passed and protected; Presentation acquisition is next.

Live Presentation acquisition passes. One Presentation tap produced `PPTX downloaded for 35 medicines at 25/07/2026, 01:38:17 Africa/Nairobi`, the browser confirmed one 220.07 KB file, and Android Downloads showed exactly one new 220 KB presentation above the protected DOCX, PDF, XLSX and CSV entries. The shared local/pharmacy-isolated/canonical/zero-AI boundary remained unchanged and OpenAI showed zero tokens and requests. The PPTX was not opened and Print was not tested. Presentation acquisition is passed and protected; the next checkpoint opens the Print-ready browser view only without initiating device printing.

Live Print-view acquisition exposed a blocking mobile-readability defect. The correct 35 canonical rows opened, but the generated page omitted a mobile viewport and forced the complete 12-column landscape table into a miniature `about:blank` view. Values were not practically reviewable by a pharmacy owner, so Print does not pass. The shared print renderer now separates screen review from paper output: phones receive readable labelled medicine cards, clear pharmacy/branch/location/time context, a record-count and zero-AI assurance, and sticky Print/Close controls; printing restores the compact A4-landscape table with repeating headers. The preview opens from a named local Blob document instead of document-writing into `about:blank`. Canonical data, pharmacy isolation and deterministic zero-AI formatting are unchanged. Re-deploy and repeat Print-view acquisition only; do not initiate device printing yet.

Live evidence after `d37cd31` passes Print-view readability: the named MS2.0 preview showed the active pharmacy, Main/Kenya, `25/07/2026, 02:08:58 Africa/Nairobi`, 35 canonical/pharmacy-isolated/local/zero-AI records, large persistent Print/Close controls, readable labelled fields, truthful dashes for missing values, and both first and last canonical medicines. Review still required an excessive 35-by-12-field scroll. The shared screen renderer is therefore refined to a compact searchable medicine index: name, strength/form/unit, stock and selling price remain immediately scannable; the first medicine starts expanded; any medicine opens to all canonical fields; local search covers name, supplier, barcode and shelf. Actual print output still restores all rows/columns as the A4-landscape repeating-header table. Re-deploy and repeat only the compact Print-view acquisition before device printing.

Live evidence after `6600418` passes compact Print review and typed fallback: 35/35 summaries were readable through the final Paracetamol record, the first Cefixime record started expanded with complete truthful fields, `Paracetal` narrowed locally to exactly one Paracetamol, expansion preserved its 500 mg/tablet/KES 10/cost 5/stock 100/EastCare/PAR-500C/2029-06/D3 values, and clearing restored 35/35. Print inventory remains untested.

Permanent typing-last rule: applicable workflows must prefer one-tap deterministic action, existing shared barcode/scanner, shared voice, appropriate camera/photo recognition, and compact recent/frequent/filtered/suggested choices before complete typed fallback. The new shared pharmacy-scoped medicine finder indexes canonical name, aliases, safe spelling variants, strength, form, sale unit, barcode, supplier, shelf and batch once; supplies truthful low-stock/out-of-stock/expiring/A-Z filters; and is reused by Catalog and Print rather than duplicated. Print bridges to the existing scanner and voice routes, matches locally, consumes no LLM/API tokens, and never changes the complete paper dataset. Focused 4,200-record verification must remain below the recorded performance bounds. No new dependency or external asset was introduced; the original code and browser-runtime reuse are recorded in `ms20-main-app/provenance-registry.json`.

Live Print Finder evidence after `c637d52` blocks the checkpoint. The preview started at `0 of 35` with empty input and `All medicines`, while typed `Par` still resolved exactly one Paracetamol; Scan barcode and Speak medicine appeared inert. Root analysis found two shared adapter defects: the embedded client gave empty input score `1` but applied the non-empty minimum score `54`, and the Blob preview depended only on `window.opener`, which mobile Chrome may sever or make unsuitable for cross-tab capture. The repair gives empty/all/clear their own positive-score boundary, adds a per-preview authenticated `BroadcastChannel` with guarded opener fallback, routes requests into the existing `openLightweightCamera("barcode")` and `startVoiceCapture` roots, and returns progress, transcript and actionable permission/unavailable states to the preview. No catalog write, duplicate, global lookup, network formatter or LLM path was added. Focused local verification passes; live Print Finder remains unpassed and must be repeated before Print Inventory.

Follow-up live evidence after `db1709c` passes the count/reset repair at `35 of 35` and preserves readable summaries plus complete Paracetamol fields, but capture remains blocked. Voice first reported a denial before a native prompt; after manual permission it stayed at `Starting microphone in MS2.0…`. Barcode stayed at `Opening the shared scanner in MS2.0…`. The deeper root is browser user activation: cross-tab messaging can deliver data but cannot transfer the direct tap required by camera/microphone permission and capture startup. Print review is therefore hosted as a same-context full-screen `srcdoc` preview inside MS2.0. Its tap synchronously enters the existing shared `getUserMedia`/Web Speech and camera/BarcodeDetector lifecycles; the camera overlay remains above the preview; close/cancel stops active capture. Voice now requests real microphone permission before recognition, reports requesting/listening/processing/matched/no-match/denied/unavailable/retry states, and bounds permission/start/capture waits. Barcode returns matched/no-local-match/not-read/cancelled states. The existing canonical Losartan record and its valid EAN-13 `6161109876546` are paired with the deterministic tracked fixture at `/main-app/fixtures/barcode-losartan-50mg.png`; no catalog mutation or duplicate is needed. Local browser interaction confirms the same-context mobile preview and direct native permission request. The live checkpoint remains open.

Live evidence after `f8fe088` proves the repaired functional roots: native microphone permission and listening occur, spoken Paracetamol matches the canonical local record, the real shared barcode camera opens with Capture/Cancel/Light controls, and typed Paracetamol remains correct. Two final presentation defects keep the checkpoint open. Every voice status update called the full Main App renderer, replacing the `srcdoc` iframe and producing an irritating blank flash; the correct EAN-13 fixture was presented too small for reliable cross-screen decoding. While Print is open and camera-overlay state is unchanged, rendering now updates finder status/query directly inside the existing iframe instead of replacing it. Camera open/close still performs the required bounded full transition. The Losartan fixture now includes a full-screen responsive vector display at `/main-app/fixtures/barcode-losartan-50mg-live.html` plus a 3000×1600 downloadable PNG. Both preserve the valid `6161109876546` payload, large quiet zones, deterministic generation and existing-catalog/no-mutation boundary. Repeat voice once and barcode once; typing is already passed and need not be repeated.

Three approved dependency-ordered improvements are now permanent requirements. First, at the later restocking/supplier/inventory-intelligence stage after Export Hub format integrity, implement a maximum-three-step deterministic Supplier Order Generator: ask by voice/text, review one editable card, Send Order. Saved reorder level is the primary trigger; suggestions remain labeled until accepted; missing levels say `Reorder level not set`; pack quantities use only confirmed conversions; every line explains stock, reorder level and suggested/requested quantity; supplier grouping, unique references, duplicate protection, practical WhatsApp/email/PDF/print/share routes, auditable reorder-level history and truthful sourced Draft/Sent/Confirmed/Partial/Rejected/Dispatched/Delivered/Delayed/Cancelled states are required. Supplier viewed, expected delivery and delay claims may appear only from verifiable authoritative sources; otherwise show Unknown/Awaiting confirmation. The ordered live sequence must cover create/accept/edit/skip reorder levels, missing-level safety, low-stock/owner/supplier orders, conversions, editable review, grouping, supported sending, receipt/confirmation/partial availability/delivery/delay/dispatch/delivery, duplicate safety, history, export/print, three-step usability, isolation and zero unjustified LLM use.

Second, at the later active-sales/shared-card/cross-input stage, implement exact medicine form/unit, unit-specific price, pack conversion and stock deduction at the shared sale-processing root. The first rush-hour card must visibly prioritize medicine, form/unit, quantity, exact selling price, payment and Confirm/Correct/Cancel with a persistent concise summary; secondary traceability stays later. Ambiguous units/prices require one simple choice, missing conversions/prices remain Unknown/confirmation required, and dangerous unit-price-strength mismatches are blocked. Exact values must survive typed/voice/barcode/camera/photo/file/catalog/quick-add routes and ledger, stock, reports, history, correction, undo, offline and exports. Pack photos must locally extract every visibly authoritative brand/generic/strength/form/unit/pack/quantity/barcode/batch/expiry/manufacturer/distributor/registration/printed-price field, label image/catalog/suggested/confirmed/unknown provenance, and update rather than duplicate. Its ordered live sequence is the focused 30-case unit/price/conversion/photo/shared-root matrix recorded in the Live App Test Plan; passed unrelated acquisition tests stay closed.

Third, through the current remaining Export Hub tests and pre-launch, add shared legal/IP/privacy/compliance safeguards without redesigning the approved visual direction. All export assets, dependencies, fonts, icons, templates, sample/demo data, wording and brand elements require a machine-readable provenance/licensing record with source, version, licence, commercial/modification/redistribution rights, attribution/notices, approved use, proof, review status and owner; unknown or incompatible production assets fail closed. Exports require minimum necessary pharmacy-isolated data, role/access controls, secure sharing, retention/deletion, audit/redaction/anonymization, document ID, source/version and truthful authoritative/calculated/suggested/estimated/awaiting labels, with no unauthorized logos, copied trade dress or false endorsement. Technical tests cover every format, cross-format canonical data, isolation/zero AI, metadata/provenance, asset registration/notices, safe demo output, access/retention/audit, and controlled unregistered/incompatible-licence failure/fallback. Professional Kenya pre-launch review remains explicit for KECOBO/copyright, KIPI/trademark, ODPC/data protection, Pharmacy and Poisons Board matters, contracts, payments and security; repository inspection must never be described as full legal compliance.

## Print Finder Live Closure — 2026-07-25

Live mobile evidence after `f88fa99` closes Print Finder. Stable in-frame updates removed the blank white voice flash; one spoken `Paracetamol` resolved exactly one saved Paracetamol and reset to `35 of 35`. The shared scanner captured the committed EAN-13 fixture `6161109876546`, resolved exactly one existing Losartan 50 mg record, exposed its saved stock 39, prices, supplier, batch and expiry, and reset to the unchanged 35-record catalog. No medicine was created, changed or duplicated, and no AI/API path was used. Protect typing, voice and barcode finding from repetition without regression evidence. The next isolated checkpoint is the actual `Print inventory` action and device print dialog; finding/filtering must not subset or mutate the complete 35-record print table.

Live Android evidence closes the native Print inventory action. The first invocation reached the system print preview; Android initially selected portrait despite the document's A4-landscape request, so the owner selected the native landscape control. The regenerated preview contained two selected landscape pages: page 1 showed the inventory title, pharmacy identity, generated time, repeated table headings and the main canonical table; page 2 retained the remaining medicine rows. No printer was selected, no physical print or PDF save occurred, and the catalog remained unchanged. Print acquisition is passed and protected. The next Export Hub stage is original/cross-format canonical consistency and integrity verification; prepare and verify that boundary before issuing another owner action.

The Export Hub final-integrity repair keeps `ms20.inventory-export.v1` as the one immutable pharmacy-scoped dataset and changes presentation at the shared renderer boundary instead of altering canonical rows. The root lesson is that a complete 12-field flat table is appropriate for CSV/XLSX but becomes unreadable when copied unchanged into PDF, Word, Presentation and Print. CSV remains escaped UTF-8 tabular data; XLSX now has stronger row heights, widths, frozen/filterable headers, hidden gridlines and typed numeric formatting. PDF uses five balanced seven-record landscape pages with three readable operational/traceability lines per medicine. Word uses seven landscape pages with five complete medicine groups per page. Presentation uses a title plus seven five-record slides with 16-point inventory text and grouped identity, prices and traceability. Print uses four explicit 9/9/9/8-record landscape sheets, repeated identity/headings and a traceability row per medicine. Fresh filenames include the generation date and exact UTC time so new validation files cannot be confused with older downloads.

Automated and visual QA over a controlled 35-record pharmacy snapshot passes: every format contains the same 35 unique names and all 12 canonical fields, Office/PDF structures open in their validators, XLSX visually renders without clipping, all eight PPTX slides render without overflow, all five PDF pages render balanced and unclipped, DOCX opens through `python-docx` with 35 complete medicine/detail/trace blocks and six explicit page breaks, and Print contains four balanced sheets plus all 35 mobile review cards. Finder-scale performance remains 4,200-record build under one second and exact search under 150 ms. Source inspection and protected verification confirm no fetch/OpenAI/LLM route. Export Hub remains open only for the owner's concise fresh-download/open visual confirmation; do not return to passed acquisition tests or advance to another major route before that evidence.

Fresh mobile Excel evidence on 2026-07-26 failed the owner-workbook checkpoint. The 35-row canonical data was still present, but the overview placed several unrelated KPI label/value pairs across two wide rows while freezing column A. Horizontal phone scrolling therefore kept labels such as `Total medicines` visible beside values belonging to total stock, low stock and suppliers, producing false owner-visible combinations such as 714, 0 and 8 medicines. This is treated as an integrity failure, not merely a viewer quirk.

The shared repair makes the export snapshot fail-closed and immutable before XLSX rendering. Medicine/finder counts, mandatory medicine identity, duplicate identity, non-negative numeric fields and recomputed medicine/value/low-stock/expiring-soon summaries must reconcile or generation throws. Every worksheet records the same source count and is validated against the snapshot before packaging. Full Inventory, Expiry Tracking and Suppliers must each contain every canonical medicine exactly once; Low Stock and Attention may contain only snapshot identities and their projection counts must reconcile. Identical input and generation time must produce byte-identical XLSX output.

The mobile-first workbook now uses four vertically stacked overview KPIs only: Total medicines, Total stock value, Low stock count and Expiring soon count. Attention Required is limited to Medicine, Stock, Expiry and Reason. Low Stock is limited to Medicine, Stock, Reorder level, Expiry and Reason. Expiry Tracking contains Medicine, Expiry, Batch and Stock for all medicines. Suppliers contains Supplier, Medicine, Stock and Shelf for all medicines. Full Inventory alone retains all 12 canonical fields. Structural inspection, formula-error scanning and visual renders of all five sheets pass over a controlled 35-record snapshot. Excel remains failed until one fresh owner-device workbook proves these changes; PDF remains blocked.

The next owner-device evidence kept Excel open on presentation only. Freezing the medicine column created an awkward split-screen effect, and horizontal movement clipped or separated overview headings, metadata and values even though the reconciled data was correct. The final focused design removes every XLSX pane and frozen column, keeps Overview within a compact four-column phone width, increases wrapped metadata/header heights, merges empty-state guidance across the used width, and reorders Full Inventory to Medicine, Strength, Form, Unit, Stock, Selling price, Cost price, Expiry, Supplier, Shelf, Batch and Barcode. Operational sheets retain their narrow dedicated columns and filters. A visual QA pass caught and fixed a too-narrow stock-value cell and clipped Full Inventory generation metadata before release. All five final renders now show complete headings and values with no pane XML, while the 35-medicine snapshot, totals, deterministic bytes and zero-AI boundary remain unchanged. One final fresh owner-device Excel validation is still required; PDF remains blocked until it passes.

## Excel worksheet identity and navigation repair — 2026-07-26

The next Android viewer test still failed owner comprehension. Its `1/5` through `5/5` control was worksheet pagination, but pages 2–5 could appear to repeat the Overview or expose a wide blank canvas. The root was insufficient worksheet identity plus workbook metadata and used-range/print metadata that let a limited mobile viewer present sheet changes ambiguously.

The editable workbook now has exactly five plainly named tabs and five matching visible A1 titles: `Overview` / `Pharmacy Overview`, `Full Inventory`, `Low Stock`, `Expiry Tracking`, and `Suppliers`. Overview includes a numbered Workbook contents guide and seven reconciled owner metrics. The four working sheets each begin with their own title, pharmacy identity, generation metadata and purpose-specific table. Their used ranges are explicit and bounded to real content; no sheet contains panes or frozen columns; no print/page metadata creates false pages; the package advertises the five worksheet titles in extended properties.

Controlled 35-record verification confirms exact sheet order/titles, distinct schemas, exact dimensions, all 35 medicines in Full Inventory/Expiry Tracking/Suppliers, reconciled summary and derived values, Low Stock projection integrity, deterministic identical-input bytes, zero formula errors, and visually distinct unclipped renders of all five sheets. Excel remains failed until one fresh live-device workbook proves the actual tab selector exposes those five names and each tab opens its corresponding content. PDF remains blocked.

## Excel in-workbook navigation repair — 2026-07-26

The next live test proved that correct tabs and titles were not enough when the Android viewer hid its sheet selector behind a confusing `1/5` control. The workbook now carries standard internal Excel hyperlinks as its primary navigation. Overview replaces the descriptive contents list with five large tap targets linked to each worksheet at A1. Every detailed sheet adds `← Back to Overview` plus a five-item navigation strip, with the active worksheet visually identified. These are package-native worksheet locations only: no macros, scripts, external URL or AI path exists.

All worksheet views request hidden gridlines and hidden row/column headings, start at A1 with A1 selected, and request 90% zoom. Used ranges remain bounded to the actual title/navigation/table content and print/page metadata remains absent. Limited third-party viewers may ignore the heading/gridline/zoom preferences or permit overscroll; MS2.0 cannot control their toolbar, page badge, advertisements or edit button.

Automated verification requires every worksheet to link to all five existing worksheet names at A1, validates 29 internal links, rejects malformed or nonexistent targets, and retains the 35-record reconciliation, formula scan and deterministic byte equality. Updated visual renders of all five sheets are distinct and unclipped. Excel remains pending until one fresh owner-device workbook proves the viewer follows these standard internal hyperlinks. If that viewer does not support them, record a viewer compatibility limitation rather than redesigning XLSX again, preserve the correct desktop workbook, and move to PDF as the phone-first owner copy.

## Excel compatibility closure and PDF owner copy — 2026-07-26

Excel Owner Workbook is passed with a documented third-party-viewer limitation. Package inspection and an Excel-compatible importer/renderer confirm five visible, distinct worksheets (`Overview`, `Full Inventory`, `Low Stock`, `Expiry Tracking`, `Suppliers`), 29 valid internal worksheet links including every Back to Overview target, exactly 35 Full Inventory medicines, reconciled summaries, valid OOXML structure, zero formula errors and deterministic identical-input bytes. The tested advertising-supported Android viewer displays the workbook but does not activate standard internal worksheet hyperlinks. Its `1/5` navigation, advertisements, row/column headings and blank-space movement are external viewer behavior that an XLSX file cannot redesign. The workbook is intended for Microsoft Excel, Google Sheets or another proper spreadsheet application; do not claim that its links work in the tested third-party viewer.

Export Hub now tells owners to choose Excel for searching, filtering, editing or analysis and PDF for the easiest phone reading and sharing. A successful Excel download adds the short recommendation to open the file in Microsoft Excel or Google Sheets.

PDF Owner Copy is the active live checkpoint. It uses the unchanged immutable export snapshot and generates one portrait Pharmacy Overview followed by seven portrait inventory pages containing five complete medicines each. Automated checks require deterministic bytes, all 35 medicines, portrait page geometry, eight valid pages and zero network/AI formatting. All eight rendered pages pass local visual inspection without clipping. The next owner action is one fresh PDF download and a simple first-page, first-inventory-page and last-page readability check.

## PDF Owner Copy closure and Word checkpoint — 2026-07-26

Authoritative Android screenshots pass PDF Owner Copy. The phone opened all eight portrait pages without horizontal scrolling; the Overview showed the reconciled 35 medicines, 714 units, KES 22,437 retail value, KES 19,677 cost value, KES 2,760 potential margin, zero low-stock and zero expiring-soon counts; seven inventory pages showed five intact medicine cards each through medicines 31–35. This real-device evidence closes PDF and must not be repeated without a new regression.

The focused final polish preserves that architecture. Important missing Stock, Selling price, Cost price and Expiry values now say `Not recorded`; optional missing Supplier, Batch, Shelf and Barcode fields are omitted; real zero remains `0`. Medicine hierarchy, five-card page breaks and portrait geometry are unchanged. The visible footer is shortened to `Generated by MS2.0 | Pharmacy inventory | [Kenya time]`, while deterministic, pharmacy-isolated and zero-routine-AI protections remain in code, metadata and verification. Automated checks prove all 35 names occur exactly once, zero/missing behavior is distinct, eight pages remain, identical input is byte-deterministic, and all eight final renders are unclipped.

Word Owner Document remains the active Export Hub checkpoint after the first owner-device usability failure. Its permanent shared DOCX architecture now emits one portrait owner-review overview plus nine portrait inventory pages with four non-splitting editable medicine cards per page. Each card separates identity, prices, supplier/traceability, prominent stock and dedicated owner notes/corrections space. The immutable snapshot still supplies exactly 35 medicines and reconciled totals; deterministic generation, pharmacy isolation and zero-AI formatting are unchanged. Microsoft Word desktop rendering confirms the intended ten-page flow without clipping.

## Word Owner Copy closure and Presentation checkpoint — 2026-07-27

Authoritative owner-device screenshots pass the redesigned Word Owner Copy layout. Replit pulled `5567f92`; the fresh 100.60 KB DOCX showed one readable overview page plus nine portrait review pages, four intact medicine cards per review page, all 35 medicines, and medicines 33–35 on page 10. Medicine names, stock and prices are prominent; supplier and traceability remain secondary; notes/corrections panels are visible. Excel and PDF remain passed and protected.

The focused final Word polish keeps the accepted architecture. Missing stock now uses the calmer 11-point `Stock not recorded` style instead of the 17-point real-stock value style; numeric zero remains a prominent `0`. Every card now includes the subtle editable prompt `Add note or correction here` plus three restrained writing lines. The owner-facing layout version is recorded only in DOCX metadata as `ms20.word-owner-copy.v2`. Microsoft Word renders the exact production artifact as ten unclipped pages. A structural editability round-trip replaced one prompt with typed text, saved and reopened the DOCX, retained all 35 medicine identities exactly once, and confirmed no document protection, macro or flattened-image dependency. Word Owner Copy is passed and must not be repeated without material regression evidence.

Presentation Owner Briefing is the next active Export Hub checkpoint. It is for a clear large-screen briefing to owners, staff or suppliers, not editable note-taking or spreadsheet analysis. The shared PPTX renderer now produces one minimal title slide, one reconciled overview slide and seven five-medicine review slides. All 35 canonical medicines, prices, stock, supplier and traceability values remain sourced from the same immutable snapshot. Microsoft PowerPoint opens and renders all nine slides without clipping; deterministic generation, pharmacy isolation and zero-AI formatting remain protected. The next live action is one fresh Presentation download and a concise title/overview/first-inventory/final-inventory visual check.

Final owner-device evidence after `c561003` permanently closes Word Owner Copy. The fresh 104.43 KB production DOCX retained all ten pages and 35 medicines; calm `Stock not recorded` labels, prominent real stock values, editable note prompts, usable notes areas, stable page flow and unclipped medicine cards are visible throughout pages 1–10. Word is passed and protected; do not reopen it without genuine regression evidence.

The Export Hub now enforces one permanent purpose-first architectural question before any format is implemented, redesigned or approved: why would a pharmacy owner deliberately choose this format instead of every other export? Each entry in the shared format registry has a unique machine-readable purpose and concise owner-facing explanation. Excel owns analysis/operations, PDF read-only sharing, Word editable review/correction, CSV machine exchange, Presentation business briefings and Print an immediate physical working copy. A format without a distinct owner workflow must be redesigned or rejected. Canonical parity, deterministic generation, pharmacy isolation, zero-AI formatting and legal/IP safety remain shared across every purpose-specific renderer.

Owner-device evidence on 27 July 2026 confirms that PPTX bytes download and Android associates the file with a slide editor, but the selected generic ad-supported Office app reports error (4). Presentation Owner Briefing remains **pending**. Exact-package validation found no truncation, missing relationship, missing content type or invalid slide count, and the generated artifact opens as nine slides in Microsoft PowerPoint. The observed error is classified as a limitation of that generic Android viewer.

The deck is now a deterministic nine-slide owner decision briefing rather than a 35-medicine inventory dump. Production rejects truncated or structurally incomplete PPTX packages before download. Export messaging updates one durable Export Hub card and keeps pharmacy-scoped, newest-first metadata history inside Export Hub; no generated binary is retained.

## Launch-roadmap reorganization — 2026-07-29

Owner direction replaces automatic ascending checkpoint execution with the Launch Critical, Demo Mode and Continuous Improvement structure in `launch-readiness-roadmap.md`. The master remains the canonical registry and historical regression sequence. All 33 non-protected/deprecated legacy checkpoints are mapped exactly once; eight approved launch-program checkpoints MS2-LT-077–084 register Multiuser Pharmacy V1, Impala Loyalty V1, Impala Community V1, measurable device/network reliability, multi-medicine photo onboarding, daily assistant V1, Demo Mode certification and subscription/multiuser billing clarity.

The Launch Gate is BLOCKED and uses no percentage. Protected checkpoints remain closed. The dependency map selects MS2-LT-054 as the single active milestone because its prerequisites are protected and it is the smallest unresolved transaction-safety boundary; selection is independent of the former numeric order. After focused implementation/verification, issue one owner test and wait.

## MS2-LT-054 implementation readiness — 2026-07-29

Repository inspection confirmed that terminal payment failure already closed only its own request, preserved stock/paid truth and emitted no chat message. The remaining owner-visible gap was the notification action: `Review payment` had no target, so the shared Notification Card could not render it. The deterministic notification root now creates a stable `payment:<transaction-id>` target for failed/cancelled outcomes; the shared action handler routes it to the existing Payment Queue. Transaction alerts remain durable through notification projection rebuilds and unchanged repeated events deduplicate without resetting their original time.

Focused TCE, payment UI, notification lifecycle, voice-viewport and consistency verifiers pass. MS2-LT-054 remains `Implemented — awaiting owner live test`; no PASS is claimed. The next action is exactly one simulator failure test with baseline stock, the durable unread Payment alert, Review payment routing, failed history and unchanged stock as evidence.

## MS2-LT-054 owner closure — 2026-07-29

MS2-LT-054 is PASS / PROTECTED. The 24 attached owner screenshots are one chronological evidence package, not two competing tests.

The preliminary Zinc Sale 1 execution proves the supporting payment route: quantity 1 M-Pesa entered Waiting at KES 70, one Simulate failed action produced failed history, and one actionable Sale 1 notification stated that stock and paid records were unchanged. Zinc’s Current stock was blank, so it cannot prove a numeric stock assertion.

The owner correctly changed to a suitable fixture rather than treating the checkpoint as failed. Septrin Sale 2 is authoritative: the visible catalog record showed suspension/bottle, Current stock 12, selling price KES 180 and buying price KES 120. Voice review resolved Septrin, quantity 1 and M-Pesa. Payment Queue showed one Waiting item and stated stock changes only after confirmation. One Simulate failed action returned the queue to zero waiting; history retained `Septrin x1 · KES 180 · M-Pesa · failed`; one separate unread Sale 2 alert stated Septrin was not completed and stock/paid records were unchanged; Review payment returned to that failed record; final Current stock remained 12.

The two unread alerts are correct because their IDs and sales differ: Sale 1 Zinc and Sale 2 Septrin. No duplicate Septrin alert is visible. Owner and automated evidence jointly protect waiting/failed stock preservation, absence of paid completion, durable failed history, one alert per unique failed sale, correct routing, queue removal, distinct-sale isolation and same-event idempotency.

The unsuitable Zinc selection exposed a test-planning defect, not a product failure. `ms20-main-app/SALE_LIVE_TEST_FIXTURE_STANDARD.md`, `fixtures/launch-sale-test-medicines.json` and `saleTestFixture.js` now require scenario-specific catalog preflight and visible before/after stock. Sale-related tests must use the production shared SaleCard and record incidental shared-root friction. The next dependency-valid Launch Critical milestone is MS2-LT-049; it is identified but not started.

## Production Sales Card owner hold — 2026-07-29

At the owner's direction, milestone execution is paused before MS2-LT-049. Historical owner requirements were reconciled into one shared Production Sales Card: canonical medicine, exact form/unit, quantity, unit-specific price, expected total, payment, stock consequence and terminal state are first-class; ambiguity is an explicit choice; Unknown price/conversion and unsafe form/unit/strength/stock combinations block confirmation. Typed sales no longer bypass review. Voice review no longer creates `VoiceReviewCard`; Payment Queue and recovery project the same shared sale model. Notifications carry the exact failed payment consequence.

The production standard has deterministic regression coverage, but it is not owner-approved or protected. The only next action is a complete owner live test with screenshots. Do not start MS2-LT-049 or any other milestone until explicit approval.

### Recovery after rejected first live attempt

Owner screenshots reject the first implementation: `paracetamol cash` became one unresolved medicine string inside `MedicineMatchCard`, leaving quantity, payment and canonical catalog facts blank. The visual shell existed, but the deterministic sale intelligence underneath it did not. The grammar had required quantity, so the no-quantity command fell through to medicine learning.

The root parser now removes terminal Cash/M-Pesa/Credit/Mixed before medicine matching, defaults an explicit-payment sale to quantity 1, supports spaced and compact quantities, and sends typed and voice equivalents through the same catalog-first match. The canonical card hydrates known stock, strength, form, unit, unit-specific selling price, calculated total, stock consequence, buying price and traceability. Its single renderer now owns Fast action, Stock & details and Traceability; a valid sale confirms from Fast action while optional trace fields remain non-blocking. Correction recalculates derived facts and readiness without rerendering or losing focus.

This is implementation evidence only. Production Sales Card remains **NOT OWNER-APPROVED / NOT PROTECTED**. MS2-LT-049 remains not started.
# Production Sales Card simplification hold — 2026-07-29

The recovered Production Sales Card information and parser are implemented, but owner evidence rejected the long all-input presentation before completing the live test. The shared renderer now uses a compact cream approval view with details on demand and correction-only editable fields. Contextual field microphones reuse the Catalog pattern and shared capture root. Status remains **AWAITING OWNER LIVE TEST / NOT PROTECTED**; do not advance to MS2-LT-049 or any later checkpoint.

Owner screenshots on 2026-07-30 accept the compact direction and all three sections, but keep the checkpoint open because every inline action rerender moved the conversation to the bottom. The shared render root now distinguishes inline active-card mutation from a genuine appended message, preserves the card/scroll coordinates through button, tab, speech, validation and correction activity, and never focuses the composer. Small Sale field microphones reuse the shared Catalog component/capture path. Await the continuous no-confirm mobile retest before approval.

Later screenshots confirm ordinary quantity/payment/tab stability but expose three remaining failures: voice lifecycle oscillation, removed-card fallback to oldest messages, and failure to separate packet from medicine identity. Root fixes now keep Sales voice DOM in place on its selected slide/field, use recent-conversation fallback when the removed anchor disappears, and introduce authoritative per-medicine pack conversion/pricing with safe missing-fact correction. Status remains **AWAITING OWNER LIVE TEST / NOT PROTECTED**.

The latest owner evidence confirms viewport stability, close-position recovery, compact presentation and pack-validation safety. Two shared routing defects were corrected without redesign: Slide 3 now refreshes through the same registered editable-field voice path as Slides 1–2, and the voice adapter no longer discards the parser's requested selling unit. Status remains **AWAITING OWNER SCREENSHOTS / NOT PROTECTED**; do not advance.

The next evidence set passes Slide 3 voice updates, packet/strip preservation, multi-quantity/payment parsing, safe missing conversion/price blocking, and ordinary tablet calculation. The sole remaining friction was singular `box`: an irregular-plural grammar error absorbed “1 box” into medicine identity and opened an empty new-medicine card. The shared parser now recognizes box/boxes and pharmacy-configured units before catalog resolution; the matched medicine context remains attached and only missing box conversion/price stay unresolved. Await the single Paracetamol-box owner test. MS2-LT-049 remains paused.

## Production Sales Card owner approval — 2026-07-30

The final owner screenshot package passes and protects the Production Sales Card recovery checkpoint exactly as implemented. It proves the compact cream three-tab card; stable quantity, payment, tab, typing and voice interaction; Slide 3 voice editing; full Correct/manual editing; close/cancel conversation-position recovery; separate medicine/quantity/requested-unit/payment parsing; packet/strip/box preservation; safe Unknown blocking; retained Paracetamol catalog and traceability context; natural “How many tablets are in one box?” clarification; microphone entry of conversion 12 and price KES 100; correct KES 100 recalculation; no duplicate medicine; and unchanged configured-tablet calculations.

This is PASS / PROTECTED and ends the temporary Sales Card hold. The exact current implementation is the permanent regression contract; do not redesign, split or weaken it.

## MS2-LT-049 owner closure — 2026-07-31

MS2-LT-049 is **OWNER-VALIDATED PASS / FROZEN / PROTECTED**. The owner typed `Ibuprofen 1 tablet cash`; screenshots proved Medicine Ibuprofen, Form tablet, Base unit tablet, Requested selling unit tablet, Quantity 1, Unit price KES 18, Total KES 18, Payment cash, projected stock `27 → 26`, Strength 200 mg, Buying price KES 9, Supplier Afya Wholesale Ltd, Batch IBU-200C and Expiry 2028-12. Barcode, Aliases and Notes were truthfully `Not recorded`; no incorrect Unknown value or pack-price leakage appeared; all three protected Production Sales Card tabs rendered correctly.

The sale was not confirmed, so no sale or stock mutation persisted. This evidence closes MS2-LT-049 without repeating it and adds no implementation change. Focused Production Sales Card and sale-fixture verifiers remain the regression evidence. The dependency-aware sequence now selects **MS2-LT-050 — Payment modes, splits and discounts**, whose prerequisites MS2-LT-014 and MS2-LT-049 are protected. Issue only its first focused owner test and wait for screenshots.

## MS2-LT-050 owner closure — 2026-07-31

MS2-LT-050 is **OWNER-VALIDATED PASS / FROZEN / PROTECTED**. The owner screenshot package proves one Septrin bottle Credit review on the shared Production Sales Card: Medicine Septrin, Form suspension, Base unit bottle, Requested selling unit bottle, Quantity 1, Unit price and Total KES 180, Payment Credit, projected stock `12 → 11`, Buying price KES 120, Supplier MedSource Kenya Ltd, Batch SEP-100S and Expiry 2028-09. Strength, Barcode, Aliases and Notes were truthfully `Not recorded`; no value was guessed and no incorrect unit, conversion or Unknown value appeared. Fast action, Stock & details and Traceability all displayed correctly with the protected design unchanged.

The owner did not press Confirm, so no sale or stock update persisted. This closes MS2-LT-050 without implementation changes or repetition. The dependency-aware Exact Transaction Truth route now selects **MS2-LT-055 — Refunds, returns and credits** because MS2-LT-051–054 are protected. Prepare only its first linked, non-destructive return/refund case and wait for owner screenshots.

## MS2-LT-055 missing-entry failure and root repair — 2026-07-31

Owner screenshots prove the first MS2-LT-055 attempt was blocked by the product, not the owner. Sale 1 completed as Ibuprofen x6 Cash with stock left 21, but its conversation receipt was inert text. A tap did nothing and long-press correctly invoked Android text selection because the receipt had no MS2.0 action boundary.

The shared completion root now attaches a stable sale reference to every newly completed receipt, recognizes persisted numbered receipts, and renders the receipt as a discoverable `Tap to open sale` button. Follow-up owner screenshots proved Sale 2 opened and its Return review was accurate, but that review had only Cancel and therefore exposed a second product dead-end.

The shared Sale Adjustment engine now owns Refund, Return and Credit review and confirmation. It enforces partial/full remaining quantity, blocks over-adjustment and repeat confirmation, persists a permanent linked adjustment with timestamps, Owner identity and audit event, and queues the same stable action for offline retry. Return restores proportional base stock; Refund requires an explicit money-only or money-plus-stock choice; Credit creates account credit with no false payment refund and no stock restoration. Completed adjustment receipts open their record, original sales list linked adjustments, and both navigate reciprocally while the original transaction remains immutable. Focused engine, UI, Production Sales Card, TCE and consistency regressions pass. MS2-LT-055 remains blocked only for owner confirmation. Repeat it from the beginning with Ibuprofen 1 tablet Cash, Return, review and Confirm; capture the completed Return receipt and linked original sale.

The 2026-07-31 ten-image owner package passes original case 055-A completely. Sale 3 recorded Ibuprofen 1 tablet Cash and stock 20 → 19. Return review truthfully showed Sale 3, quantity 1, KES 18 financial reversal, stock restore 1 and the original still completed. Confirm created linked Return 1; its detail retained Owner/timestamp/audit-facing facts and opened immutable Sale 3; Sale 3 listed `return 1 · KES 18`; the saved catalog independently showed Ibuprofen stock restored to 20. No part of MS2-LT-055 is closed or advanced from this evidence alone.

Repository control now explicitly freezes the order: original cases 055-A full Return (passed), 055-B money-only Refund, 055-C Refund with stock, 055-D Credit, 055-E partial/full/repeat safety and 055-F offline persistence/retry. Test one case at a time and wait for screenshots. Only after all six pass may the original programme be called complete. Then implement—never substitute—the approved deterministic direct-command route through the same adjustment engine, retain tappable receipts, and owner-test its ordered number, outcome, undo, last-sale, typed/microphone and safe-error cases one at a time. Final checkpoint closure requires both programmes. The exact next owner test is original 055-B only.

The corrected ten-image package closes original 055-B. Sale 5 completed as Ibuprofen x1 Cash at stock 18; Money only was visibly green/checkmarked; the review used everyday wording and showed Money back KES 18 / Stock added back 0; confirmation produced `Refund for Sale 5` with secondary `Refund record #3`. Immutable reciprocal linkage remained correct and the refund retained the post-sale stock effect. Do not repeat 055-B. The exact next original case is 055-C Refund with returned stock.

The same evidence records, but does not yet authorize early implementation of, the mandatory post-original usability programme: restore compact side-by-side Refund choices while retaining selected feedback; replace accumulating adjustment receipts with one updating Latest Adjustment status plus Open Latest Adjustment and complete Adjustment History; and generalize the singleton latest-plus-history contract across Catalog Activity, Export and future system statuses without deleting audit records. This programme and the previously approved direct-command programme begin only after original 055-C through 055-F pass, receive their own one-at-a-time owner tests, and must pass before checkpoint closure.

The complete 2026-08-01 owner package closes original 055-C as OWNER-VERIFIED PASSED / FROZEN / PROTECTED. Sale 2 recorded Ibuprofen x1 Cash and reduced stock 17 → 16. The green/checkmarked stock-restoring Refund review showed Money back KES 18, Stock added back 1, Cash reversal and original completed. Confirm created Refund record #5; its detail and immutable Sale 2 linked reciprocally; exactly one tablet returned and the catalog independently showed 17. No duplicate or incorrect stock effect appeared. Do not repeat 055-C. Queued status-card, compact-layout and direct-command improvements remain locked until all original cases finish. The exact next original test is 055-D Credit only.

The complete 2026-08-01 owner package closes original 055-D as OWNER-VERIFIED PASSED / FROZEN / PROTECTED. Sale 3 recorded Ibuprofen x1 Cash and reduced stock 17 → 16. Credit review showed Account credit KES 18, Stock added back 0, no cash refund and the original sale completed. Confirm created Credit record #6; its permanent detail and immutable Sale 3 linked reciprocally; the catalog independently remained at 16. No duplicate stock or accounting effect appeared. Do not repeat 055-D. The exact next original case is 055-E partial/full/repeat safety only. Its shared review now exposes previously adjusted and remaining quantities, disables quantity controls at safe bounds and gives an explicit no-change message when a fully adjusted sale is attempted. Queued compact-selector, status-history and direct-command improvements remain locked through 055-F.

The 17-image 055-E package proves the substantive partial/full safety path: Sale 4 sold Ibuprofen x3 Cash at stock 16 → 13; Return record #7 adjusted 1 for KES 18 and restored 1; the next review truthfully showed previously adjusted 1 and remaining 2; Return record #8 adjusted the final 2 for KES 36 and restored 2; both remained linked to immutable Sale 4; final catalog stock was exactly 16. The focused 2026-08-02 owner retest closes 055-E as **OWNER-VERIFIED PASSED / FROZEN / PROTECTED**: the already-fully-adjusted Sale 4 detail visibly states that no stock or money changed, Return/Refund/Credit are disabled, repeated taps open no review and create no record, linked Return records #7 and #8 remain visible, and the catalog independently remains at Ibuprofen stock 16. The repeated 16:07 protection messages visible in chat are historical outputs from the earlier attempt before the controls were disabled, not effects of this retest; they do not constitute a new failure or authorize a duplicate-message UX repair. Do not repeat Sale 4 or its split returns. Original 055-F remains ordered next but has not begun, and all post-original usability/direct-command work remains postponed. The requested blue clear-icon accent remains deferred shared theme-token/Impala Community direction only.

The complete 2026-08-02 owner package closes original 055-F as **OWNER-VERIFIED PASSED / FROZEN / PROTECTED**. A new Ibuprofen x1 Cash Sale 1 recorded online and changed stock exactly 16 → 15. While visibly offline, the shared Return review preserved the completed original sale and showed quantity 1, remaining 1 → 0, KES 18 Cash reversal and stock +1. Confirm created permanent local Return record #9; it linked reciprocally with immutable Sale 1, exposed the fully-adjusted protection, and restored catalog stock exactly to 16. After reconnection, one Sync processed the existing device backlog from queue 53 to 0. Return #9, its reciprocal links and stock 16 remained intact, with no second record, financial adjustment or stock restoration. The pre-sync count is recorded as historical queued device work rather than 53 actions from this test; because the target adjustment's local and post-sync identities/effects are unchanged, it is not a 055-F regression. Do not repeat 055-F. Original cases 055-A through 055-F are now complete and protected; MS2-LT-055 remains open only for its approved post-original status-card/layout and direct-command work, which must still proceed one owner-gated case at a time.

Post-original Direct Command case 1 is now prepared and awaiting owner evidence. One deterministic local parser recognizes only `open sale N`; the typed composer resolves an existing completed sale through the same shared lookup and opens the canonical immutable sale-detail card. A missing completed sale returns an explicit no-change message. No adjustment command, last-sale shortcut, microphone route, stock mutation or financial mutation is enabled by this case. Test only typed `open sale 1` against the existing 055-F Sale 1 and stop for screenshots.

The first Direct Command 1 owner attempt is an early **FAIL** with no mutation. Screenshots show the microphone transcript `open cell one` opened an unsaved Add new medicine card for `Open sale`; selling price remained required and no medicine was saved. Root cause: command priority existed only in `handleCommand()`, while `handleVoiceTranscript()` went directly to medicine parsing. The shared root now places `routePriorityCommand()` before every medicine parser for typed and microphone input, recognizes canonical `open sale N` plus approved shorthand `sale N`, and normalizes the bounded speech form `open cell one` inside command grammar only. Ordinary medicine names remain outside that grammar. Restart only Direct Command 1 with `open sale 1`, `open sale 4`, and `sale 1`; later commands remain blocked.

The second Direct Command 1 attempt is also an early **FAIL** without mutation. Screenshots prove `open sale 1`, `open sale 4`, and `sale 1` now route correctly and never enter onboarding, while `ibuprofen 1 tablet cash` still opens the protected Sale Review. The downstream lookup nevertheless reported each saved sale missing. Repository trace identifies one divergent line: the command route searched transient `state.transactions`, whereas working receipt and reciprocal adjustment navigation call `openCompletedSale()` over `transactionEngine.list()`, the persisted immutable ledger. Commands now use that exact shared function with no precheck. The existing collision contract selects the latest completed non-reversed ledger record for an unqualified daily Sale number; receipt/adjustment transaction IDs resolve exact historical records. Focused fixtures cover latest-number collision, explicit historical ID, fully adjusted Sale 4, synchronized Sale 1 and true missing-sale safety. Restart only Direct Command 1.

The complete 2026-08-02 owner package closes post-original **MS2-LT-055-G / Direct Command 1 as OWNER-VERIFIED PASSED / FROZEN / PROTECTED**. `open sale 1` and shorthand `sale 1` opened the same immutable Ibuprofen x1/KES 18/Cash Sale 1 with historical post-sale stock 15, completed status, fully-adjusted protection and linked Return #9. `open sale 4` opened immutable Ibuprofen x3/KES 54/Cash Sale 4 with historical post-sale stock 13 and linked Returns #7/#8. Ordinary `ibuprofen 1 tablet cash` still opened the protected non-mutating Sale Review; catalog stock stayed 16, queue stayed 0 and no new sale, adjustment, stock, financial or queue effect occurred. Do not repeat 055-G without verified regression.

The complete 2026-08-02 owner package closes **MS2-LT-055-H / Direct Command 2 as OWNER-VERIFIED PASSED / FROZEN / PROTECTED**. The owner used the microphone for `ibuprofen one tablet cash`; the shared Sale Review showed Ibuprofen tablet, quantity 1, KES 18 Cash and stock 16 → 15, then one confirmation created completed Sale 2 with stock left 15. The owner next used the microphone for `return sale 2`; the shared deterministic voice/typed priority router resolved persisted immutable Sale 2 and opened the existing Return review directly. It showed previously adjusted 0, remaining 1 → 0, quantity 1, KES 18 money back, stock +1, Cash reversal and the original sale completed. The visible review-only notice confirmed that nothing had changed and the original sale had not been deleted or edited. Confirm Return was not tapped, so no Return record, stock restoration, financial reversal or queue mutation occurred. No typing or manual sale lookup was required. Do not repeat 055-H without verified regression.

**Latest protected resume point: MS2-LT-055-H.** Preserve the review-only regression gate proving that `return sale N` resolves through the shared deterministic voice/typed command router and persisted immutable-sale resolver to the correct shared Return review without performing an adjustment.

The complete 2026-08-02 owner package closes **MS2-LT-055-I / Direct Command 3 as OWNER-VERIFIED PASSED / FROZEN / PROTECTED**. The phone first transcribed `return cell to`; this recognition error caused no sale, stock, money, adjustment or queue mutation and does not authorize unsafe broad `cell`-to-`sale` matching. Repeating by microphone produced `return sale 2`, which reopened the correct shared Return review. One tap on Confirm Return created exactly Return record #10 through the protected shared adjustment engine. The confirmed record shows Ibuprofen x1 tablet, KES 18 reversed against Cash, stock +1, confirmed status and Owner staff, and links back to immutable completed Sale 2. Sale 2 retains quantity 1, KES 18 Cash and historical post-sale stock 15, links reciprocally to Return #10, shows the fully-adjusted no-change protection and disables Refund/Return/Credit. Catalog stock is exactly 16. No duplicate record, stock restoration, financial reversal or queue effect is visible. Do not repeat 055-I without verified regression.

**Latest protected resume point: MS2-LT-055-I.** Preserve one adjustment record, one stock restoration, one financial reversal, reciprocal immutable links, remaining quantity 0 and disabled controls after full adjustment.

**MS2-LT-055-J / Direct Command 4 — Refund routing only is the only active owner case.** The shared priority router now accepts exact `refund sale N` through both microphone transcripts and typed fallback, resolves the persisted immutable completed sale, and delegates to the existing shared Refund review root. This case is review-only: do not confirm, and verify no Refund record, stock, money or queue mutation. The rejected transcript `return cell to` remains locked as no command; bounded protected `open cell one` behavior is unchanged. Stop for owner screenshots before any confirmation or later command case.

The first 055-J owner attempt is a safe early **FAIL** with no mutation. A fresh Ibuprofen x1 Cash Sale 3 completed at stock 15, and voice was correctly transcribed as `refund sale 3`, but the app displayed `Refund by command is not enabled yet. Nothing changed.` Repository comparison identifies the exact cause as a stale deployed router: that fallback is present in the revision before `2e2abaa`, while pushed `2e2abaa` contains the `direct.action === "refund"` dispatch to the persisted completed-sale resolver and shared Refund review, and the focused gate passes. This is not an adjustment-engine failure and requires no second source patch. Deploy the latest repository revision, verify it, then restart only 055-J from the beginning with a fresh sale. Do not repeat any protected checkpoint.

The second 055-J owner attempt is also a safe early **FAIL** with no adjustment mutation. The deployed app was current and a fresh Ibuprofen x1 Cash Sale 4 completed at stock 14. Voice correctly produced `refund sale number four`, but the deterministic parser accepted only `sale N`; it returned no command, so the transcript reached the ordinary medicine parser and opened an unconfirmed Sales card for medicine `refund sale number`, quantity 4. Nothing was confirmed. Root cause is the missing optional literal `number` in the shared command grammar, not voice recognition, dispatch order, persisted-sale lookup or the Refund engine. The shared parser now accepts exact `refund sale 4`, `refund sale four`, `refund sale number 4` and `refund sale number four` before medicine parsing. Incomplete commands, `refund medicine 4`, and Refund/Return `cell` substitutions remain rejected; bounded protected `open cell one` remains unchanged. Restart only 055-J with a fresh sale after deploying the new revision.

The corrected 2026-08-03 owner retest closes **MS2-LT-055-J / Direct Command 4 as OWNER-VERIFIED PASSED / FROZEN / PROTECTED**. Deployed revision `682f7ca` accepted voice transcript `refund sale number four` at the shared priority boundary and opened the dedicated Refund Sale 4 review instead of the disabled-command fallback or medicine parser. The review showed immutable completed Sale 4, Ibuprofen tablet x1, previously adjusted 0, remaining 1 → 0, KES 18 money back, default selected Money only, stock added back 0 and KES 18 reversed against Cash. The alternative stock-restoring option remained visible. The explicit review-only notice confirmed nothing had changed, and Confirm Refund was not tapped; no Refund record, stock change, financial effect or queue mutation occurred. Protect all four deterministic forms and rejection of incomplete or unsafe lookalikes. Do not repeat 055-J without verified regression.

**Latest protected resume point: MS2-LT-055-J.** No later owner case is active until explicit owner authorization.

Permanent owner rule: MS2.0 is **Voice first → fast tap/action second → typing last**, minimal-typing and three-steps-or-fewer whenever possible. Every supported typed operational command must have voice parity wherever device/platform speech is available. Transcripts and typed input converge on the same deterministic router and shared workflow roots; no voice-only business implementation and no unjustified AI routing are allowed. Typing remains a protected fallback for accessibility, noisy environments, unsupported voice, offline phone speech limitations, correction and deliberate choice. Future live-test instructions default to voice where available, then verify typed fallback/parity. Only the owner may change this rule.

<!-- VALIDATION_CONTRACT_SYNC_START -->
## Generated validation-contract reference

- Authority: `MS2.0_MASTER_LIVE_TEST_SEQUENCE.md`
- Checkpoints: 84
- Current: MS2-LT-055 — Refunds, returns and credits
- Bridge manifest: `docs/engineering-memory/bridge-validation-contract.json`
- Token policy: ACTIVE — `docs/engineering-memory/token-execution-policy.md`
- Rule: Codex and ChatGPT Bridges load the master and Engineering Traceability Index; no parallel sequence is permitted.
<!-- VALIDATION_CONTRACT_SYNC_END -->
